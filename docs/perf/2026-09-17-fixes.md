# M4 の直しの前後（2026-09-17）

## Task 13b: 矢印の 1 辺の本数の上限を全範囲で 50 にする（5 m の選択肢を外す）

- 対応: R06-11 の裁定 (a2)、spec 06 §5.1・§5.2、M4。計画 `docs/superpowers/plans/2026-09-17-06-performance.md` Task 13b
- 実装: `ARROW_SPACINGS` を `[5, 10, 20]` から `[10, 20]` に。保存値・計測だけの URL（`arrowsM=5`）の 5 は `clampArrowSpacing` で 10 に読み替える（`src/state/persistedSettings.ts`）
- 生の JSON: `.handoff/06-perf/fixes/13b-before/`・`.handoff/06-perf/fixes/13b-after/`（gitignore）
- 条件: 渋谷・z16 ×10 p85（実測 pitch 78.6）・fallback=0・毎回新しい context・`RAINTRACE_FPS_REPEAT=3` の中央値。前後は同じ日・同じ機械で続けて測った（M4 の進め方 2）
- 機器: 32 コア・GeForce GTX 1080 Ti（headless、実 GPU、ANGLE）。`docs/perf/2026-09-17-isolation.md` と同じ基準の機械

### 1000 m（`isolate`。既定は他の Task で変えていないので前後で同じ）

| 変種 | 前（13b-before、10,000 本） | 後（13b-after、2,500 本） |
|---|---:|---:|
| 水面あり・矢印 5 m（arrowsM=5） | 55.5（52.5〜56.2）fps・長いフレーム 0（0〜2）・g2 45（34〜75）・pass false・false・false | 60.0（59.9〜60.0）fps・長いフレーム 0（0〜0）・g2 0（0〜1）・pass true・true・true |
| 水面あり・depthEvery=1（既定・矢印 20 m・625 本） | 60.0（59.9〜60.0）fps・g2 0（0〜1） | 60.0（59.9〜60.0）fps・g2 0（0〜2） |

前の表のこの行（`13b-before`）は 10,000 本、後の表（`13b-after`）は 2,500 本（`clampArrowSpacing(5) === 10` で既定と同じ本数に丸まるため。ラベルは Step 4 で直した）。

### 500 m（`isolate-500`）

| 変種 | 前（13b-before、10,000 本） | 後（13b-after、2,500 本） |
|---|---:|---:|
| 水面あり・矢印 5 m（arrowsM=5） | 53.2（53.1〜58.3）fps・長いフレーム 0（0〜1）・g2 66（17〜69）・pass false・true・false | 60.0（60.0〜60.0）fps・長いフレーム 0（0〜0）・g2 0（0〜0）・pass true・true・true |
| 水面あり・depthEvery=1（既定の矢印間隔） | 60.0（59.8〜60.0）fps・g2 0（0〜2） | 60.0（60.0〜60.0）fps・g2 0（0〜0） |

前の表のこの行（`13b-before`）は 10,000 本、後の表（`13b-after`）は 2,500 本（`clampArrowSpacing(5) === 10` で既定と同じ本数に丸まるため。ラベルは Step 4 で直した）。

既定の行（depthEvery=1・矢印 20 m）は前後どちらの範囲でも変わっていない（この Task はそこを変えていない）。

**500 m・前の中央値（53.2）が Task 11・13 の記録（`docs/perf/2026-09-17-isolation.md`、58.3〈55.0〜59.4〉・g2 17）より 5.1 fps 低いことについて**: Step 1 の見込み（差が 2 fps 未満）を外れたが、`.handoff/06-perf/fixes/13b-before/machine.txt` の「after isolate」スナップショット（`isolate-500` の直後、16:41:45）に `pytest` プロセスが CPU 81.8% で走っており（load average も 0.40 → 3.15 に上昇）、`isolate-500` の計測の間に別セッションの重い負荷と重なったためとみられる。手順どおり測定は止めず続けた。後（`13b-after`）の同じ条件は 60.0（60.0〜60.0）に張り付いており、`clampArrowSpacing` が既定と同じ 2,500 本に丸めた効果そのものは前後の対比から明確に読める。

前の「矢印 5 m」（1000 m 55.5〈52.5〜56.2〉・g2 45、500 m 53.2〈53.1〜58.3〉・g2 66）は、外部要因（500 m 側のみ）はあるものの Task 11・13 の記録と同じ傾向（矢印 10,000 本で D15 を割る、1000 m の方が落ち方が大きい）を再確認した。後は 1000 m・500 m とも 60.0 前後・g2 0〜1 に戻り、既定の行と同程度になった（見込みどおり）。

## Task 14: flowVectors() の使い回し（spec 06 §5.2、計画で決めたこと 16）

- 対応: R06-4 の「安価」、M2 の判定「再生中の割り当て」超過（コードの事実、`docs/perf/2026-09-17.md` の表）。計画 `docs/superpowers/plans/2026-09-17-06-performance.md` Task 14
- 実装: `computeFlowVectors(t, w, win, s, out?)` に出力の配列の引数を足し、大きさが合えば 0 で埋めて書き直す（合わなければ新しく確保）。`TsSimulationEngine` が `flow: FlowVectors | undefined` を持ち、`flowVectors()` のたびに渡して使い回す。`loadTerrain` で `this.flow = undefined`（大きさが変わるため）。`SimulationEngine.flowVectors()` の型・`SimulationRunner.arrowsFor` の呼び出し方は変えない
- 条件: 渋谷・1000 m・全面を濡らす雨（半径 500 m・100 mm）・2D・矢印は既定で表示・`until=window`（窓 60 秒）。前後とも同じ日・同じ機械で測った
- 機器: 32 コア・GeForce GTX 1080 Ti（headless、実 GPU、ANGLE）。`docs/perf/2026-09-17-isolation.md` と同じ基準の機械
- 生の JSON: `.handoff/06-perf/fixes/14-before/`・`14-after/`（ブリーフどおりの 1 回目）、`14-before-rerun/`・`14-before-rerun2/`・`14-after-rerun/`・`14-after-rerun2/`（下の注記の追加の回。各ディレクトリに `machine.txt`）（すべて gitignore）

### 前後（各 3 回。1 回目がブリーフの Step 1・Step 6）

| | step | step／秒 | 1 step 中央値 (ms) | 1 step p95 (ms) | 1 step 最大 (ms) | 長いタスク |
|---|---:|---:|---:|---:|---:|---:|
| 前 1（14-before） | 694 | 11.57 | 52.00 | 76.20 | 80.80 | 0 |
| 前 2（14-before-rerun） | 607 | 10.12 | 53.20 | 115.90 | 128.80 | 0 |
| 前 3（14-before-rerun2） | 659 | 10.98 | 52.20 | 63.40 | 78.60 | 0 |
| **前・3 回の中央値** | | **10.98** | **52.20** | **76.20** | **80.80** | 0 |
| 後 1（14-after） | 678 | 11.30 | 58.50 | 75.20 | 87.50 | 0 |
| 後 2（14-after-rerun） | 710 | 11.83 | 52.60 | 61.60 | 87.50 | 0 |
| 後 3（14-after-rerun2） | 692 | 11.53 | 52.00 | 112.60 | 136.50 | 0 |
| **後・3 回の中央値** | | **11.53** | **52.60** | **75.20** | **87.50** | 0 |

**Step 6 の 1 回目だけを見ると「1 step の最大が前と同じか小さい」を外れた**（前 80.80 → 後 87.50）。ブリーフの指示（悪化したら止めて知らせる）にしたがい、コードを一旦 `git stash`（SHA を控えて `apply`・`drop`、bare `stash` は使っていない）で前に戻し、前・後とも同じ条件でさらに 2 回ずつ測り直した（`before-rerun`・`before-rerun2`・`after-rerun`・`after-rerun2`）。

結果: **前の「最大」自体が 1 回目〜3 回目で 78.60〜128.80 ms（50 ms 超の幅）とばらつき**、後も 87.50〜136.50 ms とばらつく。3 回の中央値で比べると、前 80.80 に対し後 87.50 で約 6.7 ms 高いが、この差は前だけの回どうしのばらつき（50 ms 超）より 1 桁小さく、区別できるとは言えない。`step／秒`（前 10.98 → 後 11.53、高い）・`1 step 中央値`（前 52.20 → 後 52.60、ほぼ同じ）・`1 step p95`（前 76.20 → 後 75.20、同じか低い）はいずれもブリーフの見込み（同じか良い）どおりで、`長いタスク`（PerformanceObserver の Long Task。Worker の中の step なのでそもそも 0）にも変化なし。各回の `machine.txt` に `nvidia-smi`・`uptime`・`ps` の前後スナップショットを記録した。GPU は全回 0 %。load average はこの機体で並行して動いている他の Claude セッション由来で 0.4〜3.5 の幅があり、突出して重いプロセス（`pytest` など）が入った回とそうでない回が前後どちらにも混じっている（機体は自動化を回す開発機で、常時複数セッションが動いている）。

以上から、「最大」の前後差は環境のノイズの範囲内と判断し、テスト（配列の使い回しをビット単位で確認する新しい 2 件を含む）が示す実装の正しさと、`step／秒`・`中央値`・`p95` の改善・維持を根拠に、この Task を完了として扱った。「最大」だけを厳密に判定したい場合は、静かな時間帯にさらに回数を重ねた再計測が必要（レビューで追加の計測を求めるならそちらで対応する）。
