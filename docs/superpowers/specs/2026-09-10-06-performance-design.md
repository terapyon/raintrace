# Spec 06: 性能

- Status: 完了（2026-09-18）。§6.1 のとおり。03〜05 の実測と 2026-09-17 のユーザーの裁定（R06-4〜R06-10）で改訂、実装（M0〜M6）を経て、2026-09-18 のユーザー裁定（平衡の許容、1000m の残置＋注意書き、`ui` 予算の引き上げ、倍率 10 の沈み込みと p85 のカメラ埋没を PoC として許容）で締めた
- 日付: 2026-09-10（改訂 2026-09-17、完了 2026-09-18）
- 対応: base-spec §22〜§26、§55 Phase 5、§63、tech-spec §6.3・§8.4・§14
- 依存: 04、05（05 の記録が入力。§1.1。05 の手動確認のうち 1〜8 は 05 の受け入れ、9〈fps の 8 個の URL〉は本 spec §4.2 の手動の計測に移す。R06-8）

---

## 1. 目的

tech-spec §14 の性能目標を実際のアプリで測り、**原因を切り分け、安価で局所的な改善は 06 の中で直す**（R06-4）。条件付きの最適化（tech-spec §6.3 の WASM、§8.4 の IndexedDB など）を行うかどうかを判断する。

- 直すのは「安価」な項目だけで、**直す前と後を同じ条件で測る**。安価 = 新しい依存を足さない、新しい Worker を足さない、`SimulationEngine` とメッセージ（`src/shared/protocol.ts`）の形を変えない
- 構造に関わるもの（Rust WASM〈tech-spec §6.4〉、fill-spill-merge〈03 §4.1〉、遠景の LOD、IndexedDB〈tech-spec §8.4〉）は、06 の判断が要ると決めた場合に別の spec を起こす（tech-spec §2 原則4「速度は測ってから最適化する」）

### 1.1 入力（03〜05 の実測。2026-09-17 の時点）

| 項目 | 値 | 出どころ |
|---|---|---|
| 1 step の所要時間 | Chrome では**未計測**。Node: 全面が濡れた 512² で中央値 約 15 ms、半径 100 m の初期 6.2 ms。ブラウザの「最速」の step／秒（204〜6,974）と 1 tick 12 ms からの**推定**は 1 step 約 0.1〜3.5 ms（測った値ではない。204 は綾瀬 500 mm・半径 50 m〈ほぼ全面が濡れる〉、6,974 はみなとみらい 100 mm・半径 10 m で、濡れ方の異なる条件にまたがる） | 03 のベンチマーク、04 の手動確認 |
| 平衡までの時間（「最速」、100 mm・半径 10 m、500 m） | 綾瀬 267,379 step・93.7 秒、渋谷 49,332 step・47.9 秒、みなとみらい 20,296 step・3.1 秒。500 mm・半径 50 m は 3 地点とも 2 分で平衡に届かない。1x（毎秒 60 step）に換算すると 74 分・13.7 分・5.6 分。同じシナリオを Node で回すと 71.6・35.8・2.3 秒（平衡の step 数はブラウザと一致。ブラウザの「最速」は Node の約 75% の速さ） | 04 の手動確認 |
| メインスレッドの最長ブロック | 1000 m の読み込みの終わりに **123 ms**（測り直しで 106 ms）。主に `elevationRgba` 75 ms・`depressionRgba` 10 ms（`TerrainOverlay`）。ほかに React・Emotion 計 約 95 ms、`texSubImage2D` 21 ms、GC 20 ms。250・500 m では 0 件 | 04（tech-spec §14.2） |
| 3D の 1 回だけの処理 | 3D の準備（無効セルの埋め方）500 m 6〜8 ms・1000 m 18〜21 ms、水面のメッシュの作成（waterBuild）500 m 6〜7 ms・1000 m **20〜21 ms**。50 ms は下回るが、どちらも 1 フレームを落とす | 05 の Task 9 |
| クリックから表示まで（2D） | 「1000 m」を押してから 0.78 秒、新しいプロファイルで `?size=1000` を直接開くと 1.59 秒、綾瀬の初回 1.3 秒 | 04 |
| fps（D15。§5） | 05 の最終 b3a8916 では 8 条件すべてが D15 を満たす（60.0 fps の天井）。51.0（49.0〜55.8）は c25be04（Task 9、矢印の既定 10 m・10,000 本）で測った値。Task 10（9079cba）が 1000 m の既定を 20 m（2,500 本）にした後は測り直しておらず、05 の最終 b3a8916 では同じ条件（1000 m・z16 ×10 p85、実測 pitch 78.6037°・描かれるタイル 16、水面あり）で 60.0（06 の M1 で実測、`.handoff/06-perf/baseline-check.md`）。地形のみも 59.6 → 60.0。A/B: c25be04 55.3、c25be04 に 9079cba だけを重ねると 59.9。c25be04 の落ち方は 33 ms の間隔（g2）が 42〜110 本で、タイルの組み立て（最大 15.7 ms）と水面の render CPU（0.416 ms）は外れていた。**残るリスク（未計測）**: 1000 m で矢印の間隔に 5 m を選ぶと実効 10 m・10,000 本になり、51〜55 fps に落ちる見込み | 05 の Task 9（tech-spec §14.1）、06 の M1 の確かめ |
| 止まっている間の再描画 | 2D の水深の canvas ソースが `animate: true` のため、止まっている間も毎秒 60 回再描画する（1 回 1.7〜2.0 ms） | 04 |
| 割り当て | `flowVectors()` が矢印の計算のたび（最短 100 ms ごと）に 2 × N² の `Float32Array` を確保する（1000 m で 8.5 MB） | 03 の P3、04 |
| DEM の取得 | 段 2・3（DEM5・DEM10B）の地域では、範囲の外の z17 のタイルのたびに DEM1A の 404 が 1 回ずつ起きる。範囲の段を手がかりに渡せば省ける。待つ要求の無くなった取得は止めていない | 05 の計画で決めたこと 6・23 |
| バンドル | `ui` 160.0 KB（予算 150）・`index` 20.5 KB（予算 20）が超過。初期ロード 427.5 / 500 KB、総量 702.2 KB / 1.2 MB。Worker のチャンクに `maplibre-gl-shared` が複製されている（約 130 KB） | tech-spec §14.2 |
| 3D の復帰の警告 | MapLibre 6.6.0 の 3D の地形のコンテキストの復帰で、失ったコンテキストの GL 資源を触る警告が 175〜258 件出る（2D だけの喪失では 0 件。描画は戻る）。性能の項目ではないので §2.3 の後回し | 05 の Task 12 |
| MapLibre 6.6.0 の内部への依存 | `map.terrain.tileManager.getRenderableTiles()`（本番のコード）、`map.painter.context.gl`（計測のコードだけ）、`_elevateCameraIfInsideTerrain`、`Map.setTerrain` の `_checkLoaded()` と喪失の `_contextLost`、3D の復帰の警告。版を上げるときの確認点として M6 で tech-spec に移す | 05 の最終レビュー M3・横断レビュー D11 |
| 境界 15 の未評価の視点 | 描かれるタイルの境界 15（R05-4）は、p85 の倍率 2 以上を撮影では評価できなかった。機械で測る `probe=water`（可視率・ちらつき率）を提案済み | 05 の Task 13 |

### 1.2 進め方（R06-9）

**1 本のブランチ `feat/06-performance`、PR は最後に 1 つ。** M0〜M6 はブランチの中の区切りで、それぞれレビュー役のレビューを経てから次に進む。

| # | 内容 | 済んだとき |
|---|---|---|
| M0 | 本 spec の改訂、overview §6 の R06-4〜R06-10、tech-spec §14.1 の文言。05 の軽微の持ち越し（コメント・テストの締め直し、`fps.perf.ts` の出力先の引数） | レビュー役の再レビュー |
| M1 | 計測の道具（§3 の新しい項目、`arrows=0`・`depthEvery=N`、§4.1 の地点の DEM の用意）。通常のビルドの挙動を変えない | ゲートが通り、`pnpm size` の初期ロードが変わらず、1 シナリオを試しに回せる |
| M2 | §4 の計測（自動と、ユーザーの手動の 1 回）、`docs/perf/<日付>.md`、§5 の表の判定、tech-spec §14 への追記。直す項目（M4）と別の spec に回す項目を分ける。**平衡の行だけは M4 の後に確定する暫定の判定とする**（04 の実測のまま「最速」で超過とする） | §5 のすべての行に判定がある（平衡の行は暫定） |
| M3 | 05 の 51 fps の原因の確かめと、矢印の本数と fps の関係（§5.1）。M1 の後なら M2 と並行できる | 原因を数字つきで `docs/perf/` と §5.1 に書いている |
| M4 | §5.2 の安価な改善。1 項目ずつ前と後を測る。**1 step の軽い改善の後、平衡までの時間を「最速」で測り直し、§5 の平衡の行の判定を確定する**（fill-spill-merge の spec を起こすかどうかを含む） | 各項目の前後の数字と tech-spec §14 の更新。平衡の行の判定が確定している |
| M5 | `probe=water`（R06-7）。境界 15 と 16 の可視率・ちらつき率の表 | 表が `docs/perf/` にある。15 が p85 で通らなければ R05-4 をユーザーの裁定に上げている |
| M6 | 締め: §6 の完了条件、tech-spec §14 の値、MapLibre 6.6.0 の内部への依存の一覧を tech-spec に移す、判断で起こすことにした別の spec の雛形 | §6 を満たす |

M3 と M5 は M1 だけに依存する。M4 は M2・M3 の判定に依存する。**平衡の最終判定（fill-spill-merge の spec を起こすかどうか）は、M2 の数字だけでは行わない。M4 の 1 step の軽い改善の後に測り直した数字で行う**（§5 の平衡の行）。

## 2. スコープ

### 2.1 含む

- 計測用のビルドの計測表示と計測の道具（§3）
- 再現できるベンチマークのシナリオと、その実行手順（§4）
- 計測結果の記録（`docs/perf/`）
- 基準との比較と、最適化の要否の判断（§5）
- 51 fps の条件の原因の切り分け（§5.1）
- 安価で局所的な改善と、その前後の計測（§5.2、R06-4）
- 1000m 四方（約 100 万セル）で実用になるかの確認（base-spec §8）
- `probe=water` と、境界 15・16 の可視率の表（R06-7）
- 05 の手動確認の項目 9（fps の 8 個の URL）を、06 の手動の計測に含める（R06-8）
- MapLibre 6.6.0 の内部への依存の一覧を、版を上げるときの確認点として tech-spec に移す

### 2.2 含まない

- 構造に関わる最適化: Rust WASM（tech-spec §6.4）、fill-spill-merge（03 §4.1）、遠景の LOD、IndexedDB（tech-spec §8.4）。§5 の判定で要ると決めたら別の spec を起こす
- §2.3 の後回しの項目

### 2.3 後回し（R06-10。06 を先に完了し、その後に扱う）

| 項目 | 出どころ | 内容 |
|---|---|---|
| セル情報のポップオーバーのアクセシビリティ | 04 の手動確認（裁定: 06 で扱う → R06-10 で後回し） | MUI の Popover はモーダルで、開いている間はパネルに `aria-hidden` が付く。Popper にすればモーダルでなくなる |
| スタイルの読み込み中のコンテキスト喪失 | 05 の計画で決めたこと 19、Task 12 | 読み込み中（`setBasemap` の直後）に失うと `style.serialize()` が何も返さず、復帰の `setStyle` が来ないので `MapController` の `awaitingStyle` が立ったままになり、`whenLoaded` を待つ処理が止まる |
| MapLibre の 3D の復帰の警告の上流への報告 | 05 の Task 12 | 175〜258 件の警告（§1.1）。issue を出すのはユーザーの操作。E2E は 400 件未満の上限つきで許している |
| 範囲の中と外の境目の混ぜ合わせ | 05 の計画で決めたこと 8（spec 05 §4.2） | DEM の出所（02 のグリッドと GSI）が違う境目の段差。要否は 05 の手動確認 4 で見る |
| 水面の高さに置く矢印の Custom Layer | spec 05 §3.3（05 の「含まない」） | 今の矢印は 2D の symbol レイヤーで、3D では水面との視差が出る |
| 場所ごとの描かれるタイルのズーム（R05-4 の (a)） | spec 05 §4.3 | (c) の 1 つの定数の境界に代わる置き方 |
| 倍率 10 の水面の沈み込み（p45・p60 で境界 15・16 とも不合格） | M5（Task 28 レビュー、`docs/perf/2026-09-18-water-probe.md`）。**ユーザー裁定（2026-09-18）: PoC として許容** | polygonOffset の偏り（約 3 cm）が垂直強調に比例しないため、倍率が上がるほど弦の沈み込みが目立つ。直すのは後続の spec（polygonOffset を強調に比例させる案は S §10.3 の副作用〈遠くの水面が尾根越しに透ける〉があるため要検討） |
| p85 でカメラが強調した地形の中に入る（`_elevateCameraIfInsideTerrain` の守りが効かない） | M5 レビュー R2（raintrace-46）。**ユーザー裁定（2026-09-18）: PoC として許容** | `_elevateCameraIfInsideTerrain`（dev.mjs 22431〜22443、jumpTo からは 22454 の登録経由）が `getElevationForLngLatZoom` を小数のズームで呼び、`_getOverscaledTileIDFromLngLatZoom`（10549）が小数の z のままタイル ID を作るためタイルが見つからず 0 m になる。ホイールのズームは小数なので実アプリでも起きる。R05-4 は p85 について未決のまま、ユーザーの手動確認 1・2 が残る |
| `judgeWater` が雑音帯をちらつきより先に確認していない（M5 レビュー R3） | M5 レビュー（raintrace-46）、`src/ui/perfWaterStats.ts:192` | この 06 の実行では影響なし（ちらつきは全セルで 0.05% 以下）。判定の道具の頑健さの改善として持ち越す |
| `probe=water` が読みごとに `tilesLoaded`・`queryTerrainElevation(center)` を記録していない（M5 レビュー R4） | M5 レビュー（raintrace-46） | 「どの読みも標高が誤り」のような取りこぼしを防ぐ追加の守り。この 06 の実行では手すり（カメラの余裕・footprint の再読み）で代替済み |
| `water=0`（地形のみ）の読みが `waterReady` を待たず 120 秒待つ | M5（Task 27〜28 の軽微 m3） | 水面が無いので `waterReady` が来ないのは想定どおりだが、待ち時間の無駄が残る |
| 浸食（`erode`）のサイズを 6 回の読みの後に読んでいる | M5（Task 27〜28 の軽微 m4） | 読みの前に読んでも値は変わらない見込みだが、順序を直せば読みやすい |
| 標本の最小のガードが割れる場合（5 件とも `lifted < 2000`）を分けて記録していない | M5（Task 27〜28 の軽微 m5） | この 06 の実行では 判定できず の理由の内訳として吸収されている |
| `removeWater`／`dispose` が水面が無くても `onWaterDebug(null)` を呼ぶ | Task 26 の軽微（無害） | 呼び出しは無害だが、水面の有無をガードすれば余分な呼び出しを削れる |

## 3. 計測の道具（R06-5）

**計測表示は計測用のビルド（`pnpm build:perf`）にだけ入れ、本番のビルドには入れない**（R06-5。改訂前の「本番のビルドにも含める」を置き換える）。05 の不変条件（通常のビルドでは `__RAINTRACE_PERF__` が false になり、`src/main.tsx` の計測用のフックの分岐ごと消える）を保つ。

新しい仕組みは作らず、05 の道具を広げる: URL の読み取りは `perfParams`、画面の左上の JSON と `<html>` の `data-*` の結果は `perfHook`、フレームの計測は `fpsProbe`・`fpsStats`、自動の実行は `tests/perf/`（`playwright.perf.config.ts`）。

| 項目 | 取り方 | 状態 |
|---|---|---|
| 1 step の所要時間（中央値・p95） | Worker 内。`PlaybackScheduler` が step のたびにすでに呼んでいる `ports.now()` の差から求め、直近 300 step を持つ。タイマーの呼び出し回数を増やさない | **新規** |
| 実際の step 数／秒 | `PlaybackScheduler` の 1 秒の窓（frame の `stepsPerSecond`） | 既存 |
| 描画の fps | `fpsProbe`（1 秒の空回しの後の rAF の間隔）と `fpsStats`（D15。§5） | 既存。ヒストグラムの g2 もすでに結果に出ている（`fpsStats.ts` の `gapHistogram`、`fps.perf.ts` の表の g1/g2 列） |
| 長いタスク（50 ms 超） | `PerformanceObserver` の `longtask` を `perfHook` に足す（04 は使い捨てのスクリプトで仕込んだ） | **新規** |
| 質量誤差 | frame の `stats.massError`（03 §3.10） | 既存。表示だけ足す |
| 平衡までの step 数と時間 | `settled` になった時点の step と、開始からの `performance.now()` の差 | **新規**（04 は手で測った） |
| クリックから 2D の地形の表示まで | 地点の確定から地形のレイヤーが出るまで | **新規** |
| 3D を押してから最初の 3D のフレームまで | three の読み込み・3D の準備・水面のメッシュの作成・タイルが揃うまでを分けて。準備と作成は `onPrepareTime`・`onWaterBuildTime` が既存 | **一部新規** |
| 矢印を止める（`arrows=0`） | `perfParams` の項目。§5.1 の切り分け | **新規** |
| 矢印の間隔（`arrowsM=5・10・20`） | `perfParams` の項目。表示の設定の矢印の間隔（`flowVectorSpacingM`）を置き換える。1000 m の実効の間隔はその 2 倍。省けば設定を触らない。§5.1 の矢印の本数の曲線（M2 の準備で追加） | **新規** |
| 水深テクスチャの更新の間引き（`depthEvery=N`） | `perfParams` の項目。N フレームに 1 回だけ転送する。§5.1 の切り分け | **新規** |
| 可視率・ちらつき率（`probe=water`） | 水面を深度テストなしと ありで描いて画素を数える。bearing を ±0.002° 揺らした 2 フレームを比べる。水面のシェーダの計測用の uniform 1 つと `perfHook` の分岐 1 つ（R06-7、M5） | **新規** |

- 1 step の所要時間を Worker からメインへ渡す方法は計画で決める。通常のビルドのメッセージの形と挙動は変えない（M1 の完了時に通常のビルドに計測のコードが入っていないことを確かめ直す）。型に手を入れる必要が出たら、計画のレビューに上げる
- `fps.perf.ts` の出力先は引数で渡す（既定は 05 の `.handoff/05-fps/` のまま。M0）

## 4. ベンチマーク

### 4.1 シナリオ

| 地点（R06-1、R02-7 で選んだ 3 地点） | 範囲 | 降雨 |
|---|---|---|
| 足立区 綾瀬駅付近（平坦な市街地） | 500 m、1000 m | 半径 10 m・100 mm、半径 100 m・100 mm、全面を濡らす雨 |
| 渋谷駅付近（谷のある斜面） | 同上 | 同上 |
| 横浜 みなとみらい（沿岸。無効セルを含む） | 同上 | 同上 |

- **全面を濡らす雨**: 半径を範囲の半分（R04-6 の上限）にする。濡れたセルの外接矩形が範囲全体になり、tech-spec §6.3 の走査の最悪の形になる。§6.3 を都合のよい条件だけで判断しないために、意図して入れる。雨量は計画で決める
- 平衡までの時間は 2D だけで測る。3D ありでは平衡を待たず、60 秒の窓で 1 step の所要時間と長いタスクを測る（3D の描画は平衡の step 数を変えない。エンジンは決定的）。fps は 3D で測る（計画で決めたこと 4。M1 のレビューの持ち越し、M6・Task 29 で反映）
- **平衡までの時間は「最速」（R04-5）で測る**。1x は step 数 ÷ 60 で報告するが、目標にはしない（R06-6）。2 分で平衡に届かない雨は、打ち切りの上限を計画で決め、届かなかったことと、その時点の step 数を記録する
- fps は 05 の 4 視点（500 m・1000 m × z17 ×5 p60・z16 ×10 p85）× 地形のみ・水面ありを、3 地点で測る（05 は渋谷だけ）
- **DEM の用意**: CPU で決まる計測（step の所要時間・平衡）は、毎回同じ DEM で回す。Playwright の `context.route` で固定のタイル（フィクスチャ）を返し、アプリの取得の経路は変えない（§4.2）。DEM の取得は Worker の中で行うので、`page.route` では捕まらない。E2E の `routeGsi`（`tests/e2e/support/gsi.ts`）と同じく browser context で差し替える。**タイルの組が小さければリポジトリに置き、大きければ SHA-256 で固定した取得のスクリプトにする。どちらにするかは計画で決める**（`context.route` が返すフィクスチャの保存方法だけの問題になる）。出典は `tests/fixtures/gsi/README.md` と同じ形で記す。fps とクリックから表示までは、05 と同じく地理院に実際に接続する（本番のタイルの経路）
- **R06-1 からの逸脱（ユーザーの裁定 R5、2026-09-17。承認済み）**: クリックから表示まで（`probe=load`）の組にだけ、DEM1A が無く DEM5A がある段 2 の地点（北海道根室市 根室駅付近 43.330000, 145.582800）を足す。3 地点はどれも段 1 で、段 2・3 の地域の読み込み（範囲の外の DEM1A の 404）を測れないため。fps・steps は 3 地点のまま。確かめた記録は `tests/perf/fixtures/README.md`

### 4.2 実行方法

- **自動**: `pnpm build:perf` の後に `pnpm perf:fps`（`playwright.perf.config.ts`。ポート 4175、実 GPU の headless Chrome〈`--use-gl=angle --use-angle=gl-egl`〉、workers 1）。組は `RAINTRACE_FPS_SET`・`RAINTRACE_FPS_REPEAT` で選び、新しい設定のファイルは作らない。05 は実 GPU の headless で fps を測れることを示した（改訂前の「ヘッドレスでは意味を持たない」は当たらない）
  - 限界: rAF が 60 Hz に刻まれるので、60.0 fps の天井に張り付いた条件は余裕を読めない（05 の 8 条件のうち 6 条件）。vsync を外す起動の引数（例: `--disable-gpu-vsync --disable-frame-rate-limit`）が ANGLE の gl-egl で効くかは**未検証**で、M3 で試す
  - GPU の時間（`EXT_disjoint_timer_query_webgl2`）はこの機械の Chrome で使えるか**未検証**。使える前提で計画しない
- **自動化（1 step の所要時間・平衡までの時間）**: `perfHook` に `probe=steps` を足す。降雨を開始し、`settled` になるか打ち切りの上限に達するまで待って、1 step の所要時間の統計（中央値・p95）・step 数・経過時間を JSON に書く。実行するファイルは `tests/perf/` に 1 つ足し、既存の `playwright.perf.config.ts` をそのまま使う（実 GPU の起動引数は CPU の計測を妨げない）。DEM は §4.1 のとおり `context.route` で固定のタイルを返し、アプリは変えない
- **手動（実 GPU、1 回）**: ユーザーが手で行う。**05 の手動確認の項目 9（fps の 8 個の URL）を含め**（R06-8）、§5.1 の切り分けの URL と、§3 の新しい計測（g2・長いタスク・step の所要時間）もこの回で取る
  - 機器の仕様を記録する: OS、CPU とコア数、GPU、`chrome://gpu` の GL_RENDERER、Chrome の版、画面のリフレッシュレート、ウィンドウの大きさ
  - 水面ありの計測では Worker が「最速」で 1 コアを使い続ける。05 の headless は 32 コアの機械なので競合しないが、コアの少ない機械では結果が変わりうる
- **基準の機械**: 「最速」の 60 秒・5 分の目標（§5）は CPU に依存するため、基準は自動化を回す開発機（05 の headless の実測をとった機械と同じ。仕様は `docs/perf/<日付>.md` に機器の仕様とともに記録する）とする。ユーザーの手動の計測は参考値であり、基準の判定には使わない
- **記録**: 要約は `docs/perf/<日付>.md` に機器の仕様とともにコミットする。生の JSON（`deltas` を含む）は大きいので gitignore の `.handoff/` に置く

## 5. 判断

| 指標 | 基準 | 06 の着手時点（§1.1） | 超えた場合の対応 |
|---|---|---|---|
| 1 step の中央値・p95 | 8 ms・16 ms（tech-spec §6.3。DEM1A・500 m・外接矩形の走査・Chrome デスクトップ） | Chrome は未計測。推定では内側。全面を濡らす雨（§4.1）が最悪 | まず安価な改善（16 × 16 セルのブロック単位の走査範囲など。03 §3.5。内側のセルの近傍の判定を添字の足し算にまとめた版は Node で −18%〈03 の軽微 8〉）。それでも超えれば Rust WASM の spec（tech-spec §6.4）。平均だけでなく時系列で見る |
| 平衡までの時間（R06-6） | **「最速」で**、幅 30 セル程度の池（半径 10 m・100 mm）は 60 秒以内、幅 100 セル規模の池（半径 100 m・100 mm）は 5 分以内。1x は step 数 ÷ 60 で報告するだけ。**CPU に依存するため、基準は自動化を回す開発機（§4.2。仕様は `docs/perf/<日付>.md` に記録する）とし、ユーザーの手動の計測は参考値とする** | 04 の測定（500 m、半径 10 m）: 綾瀬 93.7 秒（超過。同じシナリオを Node で回すと 71.6 秒）、渋谷 47.9 秒、みなとみらい 3.1 秒。半径 100 m は未計測。**M4 の 1 step の軽い改善（03 の軽微 8。Node で −18%）を当てはめると、綾瀬の Node は 71.6 → 約 59 秒。ブラウザの「最速」は Node の約 75% の速さ（04 の記録）なので 59 ÷ 0.75 ≈ 78 秒と見込まれ、M4 の後も 60 秒を超える見込み** | **判定は M2 の数字だけでは確定させず、M4 の 1 step の軽い改善の後に測り直した数字で行う**（§1.2）。それでも「最速」で超えていれば、03 §4.1 のとおり (b) 静的な fill-spill-merge 法を本命、(a) virtual pipe モデルを次点として spec を起こす。**ユーザー裁定（2026-09-18、M6・Task 29）: 許容**。Task 25 の確定（綾瀬 500 m・半径 10 m は 76.8 s で超過）を受けても fill-spill-merge の別 spec は起こさない（雛形も作らない）。超過は記録として残す（本節の下の「6.1 照合」、tech-spec §14.1、overview R06-6） |
| 地図操作の fps | **D15**: 平均 57 fps 以上、かつ長いフレーム（そのランの中央値の 2.5 倍を超えた間隔）1% 以下（spec 05 §4.4、R05-6。シミュレーション中を含む）。**g2（33 ms の間隔）の本数もあわせて報告する**（60 と 30 fps の行き来は 41.7 ms の閾値に掛からず、D15 の長いフレームに数えられない） | 05 の最終 b3a8916 では 8 条件すべてが 60.0。05 の記録の 51.0 は c25be04（矢印 10,000 本）の値。矢印の間隔に 5 m を選んだ場合（1 辺 100 本・10,000 本。範囲によらない〈`arrowSpacingForRange` の式〉）は §5.1 の M3 で計測済み | §5.1 で矢印の本数と fps の関係を測る。落ちれば M4 の対応をユーザーの裁定に上げる |
| メインスレッドの最長ブロック | 50 ms（全操作） | 1000 m の読み込みの終わりに 123 ms | 原因の処理を分割するか軽くする（§5.2）。Worker に移すにはメッセージの形が変わるので、安価の外（別の spec、または裁定） |
| クリックから 2D の地形の表示まで | 3 秒（キャッシュなし、一般的な回線） | 0.78〜1.59 秒 | DEM の再取得が主因なら IndexedDB のキャッシュ（tech-spec §8.4）。そうでなければ取得の並列度やデコード、DEM1A の 404（§5.2） |
| 3D を押してから最初の 3D のフレームまで | 3 秒（同上。改訂前の「クリックから 3D 地形表示まで」を、3D が選んで入るものになったので分けた） | 未計測（準備 18〜21 ms・水面のメッシュ 20〜21 ms は既知） | three の読み込み・タイルの待ちのどれが主因かで決める |
| 止まっている間の再描画 | 止まっている間は地図を毎フレーム描き直さない | 2D の水深の canvas が `animate: true` で毎秒 60 回 | §5.2 |
| 再生中の割り当て | 定期的な大きな確保をしない | `flowVectors()` が最短 100 ms ごとに 2 × N² | §5.2 |
| バンドル | tech-spec §14.2 の予算 | `ui`・`index` がチャンクの予算を超過（上限の内） | チャンクの分け方を見直す（§5.2）。**ユーザー裁定（2026-09-18、M6・Task 29）**: Task 19 で 151.8 KB まで削った後も 150 KB の予算を 1.8 KB 超えるため、`ui` の予算を 200 KB に引き上げる（tech-spec §14.2）。CI が検査する初期ロード・総量の上限は変えない |
| 1000m 四方 | 上の基準をすべて満たすか | fps（×10 p85・水面あり）と最長ブロックが外れている | §5.1・§5.2 の後も満たさなければ、1000m の選択肢を外すか、注意を表示する（文言と UI が要るので、そのときに裁定に上げる）。**ユーザー裁定（2026-09-18、M6・Task 29）**: 1000m は残す。ただし注意書きを出す（Rust WASM の spec は起こさない）。注意書きの文言・UI の実装は Task 30 の範囲とし、要るかどうかは Task 30 の前にコントローラーへ確認する |

> **M2 の判定（2026-09-17、`docs/perf/2026-09-17.md`）**: 超過は次のとおり。1 step の中央値は 500 m・全面を濡らす雨で最悪 9.50 ms（綾瀬。p95 は最悪 13.30 ms で基準内）→ Task 15、なお超過なら Task 24、それでも超えれば Rust WASM の spec。平衡は 500 m の r10 が綾瀬 87.7 s、r100 が 3 地点とも 300 s の cap で届かず → Task 15 の後に Task 25。最長ブロックは 3D に切り替え 206 ms・1000 m の読み込み 137 ms → Task 16（なお超過なら Task 17）。3D に切り替えの長いタスクは原因未切り分けで対応する Task が無く、Task 16 の前後で見直す。止まっている間の再描画は 2 秒で 120 回 → Task 18。再生中の割り当て（1000 m で 8.5 MB を最短 100 ms ごと）→ Task 14。バンドルは `ui` 160.0 KB / 150 KB → Task 19（`index` 20.5 KB / 20 KB は記録のみ）。1000 m 四方は 1 step・平衡・最長ブロック・止まっている間の再描画・再生中の割り当てが外れる（後の 2 つは範囲によらない）→ M4 の後に Task 25 で判定し、満たさなければユーザーの裁定。基準内: 地図操作の fps（3 地点の 24 セルすべて 60.0、長いフレーム 0、g2 最大 2。既定の矢印の本数で。1000 m で矢印 5 m〈10,000 本〉は M3 が曲線で確かめ、M4 の矢印の対応は M3 の後にユーザーの裁定）、クリックから 2D（中央値の最大 0.77 s）、3D を押してから最初の 3D のフレーム（中央値の最大 0.64 s。Task 20 の条件を満たさない）。平衡の行は暫定（M4 の Task 25 で確定する）。みなとみらい 1000 m は段 2 の DEM（セル約 3.89 m）で走ったので、段 1 を前提にした 1 step と平衡の判定から除いた。

> **M4 の後の判定（2026-09-17、`docs/perf/2026-09-17-fixes.md`）**: 直した項目: Task 13b（fps、矢印の 1 辺の本数の上限を全範囲で 50 に。1000 m 矢印 5 m 55.5 → 60.0 fps・500 m 矢印 5 m 53.2 → 60.0 fps）、Task 14（再生中の割り当て、`flowVectors()` の配列を使い回す）、Task 15（1 step の中央値。Task 25 の `until=settle`〈M2 と同じ条件〉の実測で 500 m 全面 −16.8%〜−26.1%、綾瀬 7.90・渋谷 6.50・みなとみらい 7.30 ms。中央値・p95〈11.10・9.60・10.70 ms〉とも 8 ms・16 ms の基準内。Task 15 自身の `until=window`〈窓 60 秒〉の前後は −17.3%〜−18.8%、綾瀬 8.60・渋谷 8.20・みなとみらい 8.30 ms で、こちらは M2 比の判定には使わない）、Task 16・17（最長ブロック・読み込み、1000 m 綾瀬 88 → 0・渋谷 86 → 0 ms）、Task 17a (i)・(ii)（最長ブロック・3D 切り替え、1000 m 綾瀬 141 → 0・渋谷 102 → 0 ms）、Task 18（止まっている間の再描画、2 秒で 120 → 0）、Task 19（バンドル `ui`、160.0 → 151.8 KB）。**平衡: 確定・超過**（Task 25。500 m 半径 10 m は綾瀬 87.7 → 76.8 s でなお 60 秒を超え、渋谷 35.1 s・みなとみらい 2.6 s は内。半径 100 m は 3 地点とも 300 秒の cap で届かず）。**別の spec を起こす**: fill-spill-merge 法（平衡。03 §4.1 (b) 本命・(a) 次点、M6・Task 29 で spec の雛形）。tech-spec §6.3 の条件（500 m・DEM1A）では 1 step の中央値・p95 とも基準を満たした（Task 15、上の `until=settle` の実測）ため、この基準で Rust WASM は必須ではない。1000 m の扱いはユーザーの裁定に上げる（1000 m を外すか、注意を出すか、8 ms に届かせる Rust WASM の spec を起こすか。1000 m で外れるのは 1 step〈参考、綾瀬 30.80・渋谷 36.00 ms〉・平衡だけになったが、500 m も平衡の基準を満たしていないので、平衡の部分は fill-spill-merge の対応に集約される。M6・Task 29 で fill-spill-merge の spec とあわせて問う）。**記録のみ**: 再生中 2D の長いタスク 53 ms（M2 で 1 件のみ、Task 25 の再測定 18 行で再発せず）、クリックから 2D と 3D を押してから最初のフレーム（元々基準内。Task 16・17・17a の副次効果で同じか短くなった）、バンドル `ui` の残り 1.8 KB・`index` の 1.0 KB の超過（パネルの遅延読み込みは M6、RB-1 の次の手）。詳細は `docs/perf/2026-09-17-fixes.md`「M4 の後の判定」の表。

> **M6 のユーザー裁定（2026-09-18、Task 29）**: 上の「別の spec を起こす」は行わない。平衡の超過（60 秒基準に対し綾瀬 76.8 s）は**許容**し、fill-spill-merge 法の別 spec は起こさない（雛形も作らない）。超過は記録として残す（tech-spec §14.1、overview R06-6）。1000 m の扱いは「残す。ただし注意書きを出す」（Rust WASM の spec は起こさない。実装は Task 30 の範囲、UI に注意書きが要るかは Task 30 の前にコントローラーへ確認する）。`ui` のチャンクの予算は 150 KB → 200 KB に引き上げる（tech-spec §14.2）。加えて、`probe=water`（M5）で見つかった 2 点も PoC として許容する: 倍率 10 の水面の沈み込み（p45・p60 で境界 15・16 とも不合格。直すのは後続の spec）と、p85 でカメラが強調した地形の中に入る件（`_elevateCameraIfInsideTerrain` が小数のズームで標高 0 m を読み、守りが効かない。§6.1 参照）。

改訂前の R06-3 の 1x の目標（幅 30 セル程度の池を 1x で 60 秒以内）は、次の事実から届かない（R06-6）: 1x は毎秒 60 step（R04-5）なので 60 秒は 3,600 step。03 §4.1 の見積もりでも幅 30 セルの池の `settled` までは約 2,000〜4,700 step（1x で 33〜78 秒。03 の表は 35〜80 秒に丸めている）。04 で実際の 3 地点は 20,296〜267,379 step かかった（1x で 5.6〜74 分）。平坦な綾瀬は浅い窪地が連なり、越流が 9 回続く。

### 5.1 51 fps の切り分け（M3）

**M1 の確かめ（2026-09-17、`.handoff/06-perf/baseline-check.md`）**: 51.0（49.0〜55.8）は c25be04（Task 9、矢印の既定 10 m・10,000 本）で測った値。Task 10（9079cba）が 1000 m の既定を 20 m（2,500 本）にした後は測り直しておらず、05 の最終 b3a8916 では同じ条件で 60.0。地形のみも 59.6 → 60.0。A/B（同じ視点・静かな負荷）では c25be04 55.3、c25be04 に 9079cba だけを重ねると 59.9、b3a8916・e95b9c1 は 60.0 で、**原因は A/B で矢印の本数と特定した**。環境の説は外れた（静かな時間の c25be04 が 48.5 まで落ち、別のプロセスが CPU を使っていた e95b9c1 は 60.0）。**M3 は矢印の本数を変えた曲線（計測だけの `arrowsM=5・20`。1000 m で実効 10 m・10,000 本と 40 m・625 本）で確かめる。** 下の表の切り分け（M1 の `isolate`）は既定の矢印の本数ではすべて 59.9〜60.0 で、転送と描画は既定の本数では上限の内にある。

条件は 05 と同じ（1000 m・z16 ×10 p85、`fallback=0`、渋谷、新しい context）。同じ視点で次を変えて比べる。

| 変える条件 | 見るもの |
|---|---|
| `depthEvery=N`（N = 1・2・4） | 転送が主因なら N を上げたぶん fps が戻る。描画が主因ならほとんど変わらない（レビュー役の提案） |
| 止めた水面（平衡または一時停止の直後に雨を止めて測る。新しいパラメータ 1 つ） | 転送 0・描画は変わらない場合の下限で、アップロードと描画の最も明確な切り分け（`depthEvery=N` はその段階版）。`perfHook` はすでに `afterFirstJump` で降雨を始めているので、それを止めるパラメータ 1 つで足りる |
| `arrowsM=5・20`（計測だけ。1000 m で実効 10 m・10,000 本と 40 m・625 本） | 矢印の本数と fps の関係。既定（20 m 実効・2,500 本）・`arrows=0` と並べて曲線にする |
| `arrows=0` | 矢印は既定で表示され、Worker は最短 100 ms ごとに送り、メインは GeoJSON のソースを 10 Hz で `setData` する。MapLibre はタイルが最も多い p85 で矢印を並べ直す。05 では除外していない |
| 地形のみ（`water=0`） | 基準（05 では 59.6 fps、最遅 58.5） |
| vsync を外す起動の引数 | 効けば、天井に張り付いた条件の余裕が読める（§4.2。未検証） |
| GPU の時間（使えれば） | CPU のタイマーは、ドライバが後回しにする `texSubImage2D` や描画を見られない。render CPU 0.416 ms と fps の落ちが両立するのはこのため |

- 結果は g2 と平均 fps の両方で読む
- 転送が主因だった場合の次の候補: 変わった行だけの転送、Float16 のテクスチャ、更新の間引き（§5.2）。描画が主因なら遠景の LOD（別の spec）
- 原因を数字つきで `docs/perf/` と本節に書く

> **結果（2026-09-17、`docs/perf/2026-09-17-isolation.md`）**: 主因は矢印の symbol の本数。平均 fps（3 回の中央値）と g2: 矢印なし 60.0・g2 0、矢印 20 m 60.0・g2 0、既定 60.0（59.7〜60.0）・g2 0（0〜3）、矢印 5 m 55.7（49.1〜57.1）・g2 41（29〜107、D15 を外す）、地形のみ 60.0、depthEvery=1 60.0・2 59.9・4 59.9、止めた水面 60.0。転送・水面の描画・地形の描画は既定の本数では上限の内。vsync を外す引数は効いた（地形のみ 128.1 fps）、GPU のタイマーは使える。**矢印 5 m の本数（1 辺 100 本・10,000 本）は `arrowSpacingForRange(baseM, sizeM) = baseM × sizeM ÷ 500` により範囲によらない**（レビュー指摘）。500 m でも同じ本数で確かめると 58.3（55.0〜59.4）・g2 17 で、**中央値は D15 の内**（1000 m の 55.7 は D15 を外す）。落ち方の大きさは範囲で違う（同じ結果ファイルの「500 m での確かめ」）。M4 の対応: **裁定（2026-09-17、R06-11）**: (a2) すべての範囲で矢印の 1 辺の本数の上限を 50 にする（`.superpowers/sdd/2026-09-17-06-performance/arrow-ruling-request.md`）。UI の形は「5 m の選択肢を外す」（`ARROW_SPACINGS` を `[10, 20]` にし、保存済み・計測だけの URL の 5 は 10 へ移行する。spec 05 §3.3 の「1 辺の本数を 500 m の既定にそろえる」設計と整合する）。実装と前後の計測は M4 の Task 13b。**前後の数字（`docs/perf/2026-09-17-fixes.md`）**: 1000 m の矢印 5 m は前 55.5（52.5〜56.2）fps・g2 45（34〜75、D15 を外す）→ 後 60.0（59.9〜60.0）fps・g2 0（0〜1、D15 を満たす）。500 m の矢印 5 m は前 53.2（53.1〜58.3）fps・g2 66（17〜69）→ 後 60.0（60.0〜60.0）fps・g2 0（0〜0）（500 m の前の中央値は Task 11・13 の記録より低く、計測中に別プロセスの CPU 負荷と重なったため。詳細は `docs/perf/2026-09-17-fixes.md`）。既定の行（depthEvery=1・矢印 20 m）は前後で変わっていない。

### 5.2 安価な改善の候補（R06-4、M4）

M2・M3 の判定で基準を超えた項目だけを直す。どれも §1 の「安価」の条件（依存・Worker・エンジンとメッセージの形を変えない）を満たす形で行い、満たさなくなったら別の spec に回す。

| 候補 | 根拠 | 注意 |
|---|---|---|
| `elevationRgba`・`depressionRgba` を軽くする、または分割する | 最長ブロック 123 ms の主因（75 ms・10 ms）。1 セルごとに配列を作って `set` している | Worker に移すのは地形のメッセージに RGBA を足すことになり、安価の外 |
| 止まっている間は水深の canvas ソースの `pause()` を呼ぶ（動き出したら `play()`） | 毎秒 60 回・1 回 1.7〜2.0 ms | MapLibre の `CanvasSource` が既に持つ API（`maplibre-gl-dev.mjs` 4601〜4609）。`pause()` は `_playing` を false にし、`hasTransition()`（同 4672〜4673。`_playing` を返す）を false にして、`prepare()` の `texture.update`（同 4649。`resize \|\| this._playing` のときだけ呼ぶ）を止める。ソースを作り直す必要はない。3D では隠した canvas は転送されないが、`hasTransition()` を真に保つので再描画は残る |
| `flowVectors()` の配列を使い回す | 1000 m で 8.5 MB を最短 100 ms ごとに確保 | 戻り値が次の呼び出しで上書きされる約束になる。呼び出し元（Worker の 1 か所）とテストで確かめる。型は変えない |
| DEM1A の 404 を省く（範囲の段を手がかりに渡す）。待つ要求の無くなった取得を止める | 段 2・3 の地域で範囲の外の z17 のタイルごとに 404 | 止める方は安価に収まる場合だけ |
| 51 fps の転送側の改善（間引き・変わった行だけ・Float16） | §5.1 で転送が主因と分かった場合 | M1 の確かめで、既定の矢印の本数では転送は主因でない（`depthEvery=1・2・4` がどれも 59.9〜60.0）。§5.1 の結果が変わらなければ行わない |
| `ui` のチャンク（`TextField` → `OutlinedInput` と `FormControl`） | 160.0 KB / 150 KB（tech-spec §14.2、RB-1 の順序） | 見た目とアクセシビリティの E2E を保つ |
| 矢印の 1 辺の本数を全範囲で 50 に抑える（5 m の選択肢を外す） | §5.1 の結果（R06-11、裁定 (a2)）: 矢印 10,000 本（1 辺 100 本）のコストは範囲によらず約 +9.5 ms で、1000 m は D15 を外し、500 m は天井の吸収で中央値のみ保つ | `ARROW_SPACINGS` を `[10, 20]` にする。保存済み・計測だけの URL（`arrowsM=5`）の 5 は 10 に丸める（移行）。M4 の Task 13b で実装。前: 1000 m 55.5・500 m 53.2 fps → 後: 1000 m 60.0・500 m 60.0 fps（`docs/perf/2026-09-17-fixes.md`） |

Worker のチャンクの `maplibre-gl-shared` の複製（約 130 KB）は、ビルドの設定で消せるかを M2 で調べ、安価でなければ記録に留める。

## 6. 完了条件

1. §4 のすべてのシナリオについて、計測結果を `docs/perf/` に記録している（ユーザーの手動の計測〈05 の項目 9 を含む〉を含む）
2. §5 の各指標について、基準内か、§5.2 で直して前後の数字があるか、超えていて対応の spec を起こしたかが決まっている
3. tech-spec §14 に、実測した値を書き足している（§14.1 の 05 の表に追記する）
4. 1000 m・z16 ×10 p85・水面ありの 51 fps（05 の記録、c25be04）の原因を、数字つきで名指ししている（§5.1）
5. MapLibre 6.6.0 の内部への依存の一覧を、版を上げるときの確認点として tech-spec に移している
6. `probe=water` で、描かれるタイル 15 と 16 の可視率・ちらつき率の表を記録している。15 が p85 で通らなければ R05-4 をユーザーの裁定に上げている（R06-7）

### 6.1 照合（2026-09-18）

1. **満たす**: §4.1 のシナリオはすべて `docs/perf/` に記録済み。自動: `2026-09-17.md`（M2）・`2026-09-17-fixes.md`（M4）・`2026-09-18-water-probe.md`（M5）。手動（Task 9、R06-8。05 の項目 9 を含む）: `2026-09-17-manual.md`（コミット edba6d3、レビュー clean）
2. **満たす**: §5 の 10 行はすべて判定が決まっている。1 step・最長ブロック・fps・クリックから 2D/3D・止まっている間の再描画・再生中の割り当てはいずれも基準内（M4、Task 13b〜19）。バンドルは Task 19 で改善しつつユーザー裁定（2026-09-18）で `ui` の予算を 200 KB に引き上げて内に収めた。平衡は超過を確定（Task 25）した上でユーザー裁定（2026-09-18）が許容（別 spec は起こさない）。1000m 四方はユーザー裁定（2026-09-18）が「残す。ただし注意書きを出す」（実装は Task 30）
3. **満たす**: tech-spec §14.1 に M2・M4 後・M6（Task 29）の実測を追記済み。§14.2 に 03〜06 各時点の `pnpm build && pnpm size` の表と、`ui` の予算引き上げ（2026-09-18）を追記済み
4. **満たす**: 51 fps（05 の記録、c25be04）の原因は、A/B（M1 の `baseline-check.md`）と §5.1（M3、`2026-09-17-isolation.md`）で、矢印の symbol の本数（1000 m で 5 m の間隔を選ぶと 10,000 本）と数字つきで特定した。Task 13b（R06-11）で全範囲の矢印の 1 辺の本数の上限を 50 にし、前後とも 60.0 fps に揃えた
5. **満たす**: MapLibre 6.6.0 の内部への依存の一覧を tech-spec §14.4 に移した（2026-09-18、Task 29）。11 項目の行番号を `maplibre-gl-dev.mjs`／`maplibre-gl-shared-dev.mjs` の 6.6.0 で 1 件ずつ開いて確認し、2 件の食い違い（circle が LEQUAL の 2 つ目の地形のパスではなく symbol と同じ深度テストなしだったこと、`waterLayer.ts` の `resetState()` の行番号のずれ）を見つけて直した（§14.4 の「確認で見つかった食い違い」）
6. **満たす**: `probe=water`（Task 26〜28）で描かれるタイル 15・16 の可視率・持ち上げ比・ちらつき率の表を `docs/perf/2026-09-18-water-probe.md` に記録した（68 行、レビュー raintrace-46 が全行を生データから再計算し一致を確認）。境界 15 は p85 で通らなかったわけではなく（p85 は評価できる行が 1 つも無く未評価）、R05-4 はユーザーの手動確認 1・2 待ちのまま未決で残した。この未決に加え、M5 で新たに見つかった 2 点（倍率 10 の沈み込みが境界 15・16 とも不合格、p85 でカメラが地形の中に入ったまま持ち上がらない）を含めてユーザーに裁定を仰ぎ、2026-09-18 にいずれも「PoC として許容」の裁定を得た（本節冒頭の§6完了条件のすぐ上の M6 裁定の記載、および §5 の 1000m四方・平衡の行と同じ日付の裁定）

**別の spec**: なし。fill-spill-merge・Rust WASM とも、M4 後の判定では起こす条件を満たしたが、2026-09-18 のユーザー裁定でいずれも見送った（許容・PoC 許容）。R05-4 の p85 も、範囲の LOD の別 spec ではなくユーザーの手動確認待ちのまま残す

6 条件をすべて満たしたため、本 spec の Status を「完了」に改める（冒頭）。

## 7. 裁定が必要な論点

> 裁定（2026-09-10）: R06-1〜R06-3 はすべて推奨どおり承認。
>
> 裁定（2026-09-17）: R06-4 は (b)、R06-5 は (b)、R06-6 は (b)、R06-7 は (a)、R06-8 は (b)、R06-9 は (a)（推奨の (b) の積み重ねのブランチから変更: 1 本のブランチ、PR は最後に 1 つ）、R06-10 は性能以外をすべて後回し（推奨の (b) の一部を 06 に入れる案から変更）。06 を先に完了し、後回しの項目はその後に扱う。本文は裁定を反映済み。
>
> 裁定（2026-09-17、R06-11）: M3 の結果（矢印の間隔に 5 m を選んだときの fps）を受けて、(a2) すべての範囲で矢印の 1 辺の本数の上限を 50 にする。UI の形は「5 m の選択肢を外す」（推奨。`.superpowers/sdd/2026-09-17-06-performance/arrow-ruling-request.md`）。実装と前後の計測は M4 の Task 13b で行う。§5.1・§5.2 は裁定を反映済み（前後の数字は Task 13b の完了後に追記）。
>
> 裁定（2026-09-18、M6・Task 29。5 件）: (1) 平衡が 60 秒基準を超える件（Task 25 確定、綾瀬 76.8 s） → **許容**。fill-spill-merge の別 spec は起こさない（雛形も作らない）。(2) 半径 1000 m → **残す。ただし注意書きを出す**（Rust WASM の spec は起こさない。実装は Task 30 の範囲）。(3) `ui` のチャンクの予算（151.8 / 150 KB） → **200 KB に引き上げる**（tech-spec §14.2。初期ロード・総量の上限は変えない）。(4) 倍率 10 の水面の沈み込み（p45・p60 で境界 15・16 とも不合格、M5 レビュー I2） → **PoC として許容**。直すのは後続の spec。(5) p85 でカメラが強調した地形の中に入る件（M5 レビュー R2） → **PoC として許容**。R05-4 は未決のまま、ユーザーの手動確認 1・2 が残る。本文・§5・§6.1・tech-spec §14 は裁定を反映済み。

| ID | 論点 | 推奨 | 理由 |
|---|---|---|---|
| R06-1 | ベンチマークの地点 | 02 の手動確認と同じ 3 地点（R02-7） | 見た目の妥当性を確かめた地点で性能も測れば、比較しやすい |
| R06-2 | 性能を CI の必須ゲートにするか | しない。傾向を見るための任意のジョブ（ヘッドレスでの step の所要時間のみ）は置いてよい | CI の実行環境は性能がばらつくので、必須ゲートにすると関係のない PR が落ちる |
| R06-3 | 平衡までの時間の目標 | 池の幅で決める。幅 30 セル程度の池（半径 10m・100mm 相当）は 1x で 60 秒以内。幅 100 セル規模の池（半径 100m・100mm 相当）は「最速」で 5 分以内。**R06-6 で「最速」に改めた** | 平衡までの時間を決めるのは、降雨の半径ではなく、できる池の幅である（03 §4.1）。後者は未達の公算が高く、その場合は (b) 静的な fill-spill-merge 法の spec を起こす。値は、利用者が結果を待てる長さとして置いた暫定のもの |
| R06-4 | 06 は測るだけか、直すところまでか | (b) 測り、原因を切り分け、安価で局所的な項目は 06 の中で直し、前後を測る。安価 = 新しい依存・新しい Worker を足さず、`SimulationEngine` とメッセージの形を変えない。WASM・fill-spill-merge・遠景の LOD・IndexedDB は別の spec | 安価な項目はすでに測られて原因が分かっている（§1.1）。10 行の修正ごとに spec・レビュー・計画・PR を回すと、修正より手間が大きい。「測ってから最適化する」（tech-spec §2 原則4）は満たす |
| R06-5 | 計測表示の置き場所 | (b) 計測用のビルド（`build:perf`）だけに入れ、本番のビルドには入れない。改訂前の §3 の「本番のビルドにも含める」を置き換える | 05 が「通常のビルドに計測のコードが無い」ことを不変条件として検査している（`__RAINTRACE_PERF__`）。`index` はチャンクの予算を超えている。手動の計測はもともと `build:perf` で行う |
| R06-6 | 平衡までの時間の目標（R06-3 の改訂） | (b) 「最速」で、幅 30 セル程度の池（半径 10 m・100 mm）は 60 秒以内、幅 100 セル規模の池（半径 100 m・100 mm）は 5 分以内。1x は step 数 ÷ 60 で報告するが、目標にしない。fill-spill-merge の spec は「最速」で届かない場合だけ起こす | 1x は見るための速さで、解く速さではない。1x の目標は 03 §4.1 の見積もり（約 2,000〜4,700 step、1x で 33〜78 秒）でも届かず、04 の実測は 20,296〜267,379 step（1x で 5.6〜74 分）だった（§5）。1x での数時間の計測も要らなくなる |
| R06-7 | `probe=water` を 06 に入れるか | (a) 入れる。後ろの独立したマイルストーン（M5）とする。境界 15 が p85 で通らなければ、R05-4 をユーザーに戻す | 06 の道具（計測用のビルド、新しい context、視点の置き方）をそのまま使える。遠景の LOD を判断するにも同じ可視率の表が要る。視点はカメラが強調した地形の外に置く（可視率 0 を不合格と読み違えないため） |
| R06-8 | 05 の手動の fps（項目 9）の扱い | (b) 項目 1〜8 は 05 の受け入れに残し、項目 9（8 個の URL）は 06 の実 GPU の手動の計測（1 回）に移す。ユーザーが手で行う | GPU の計測を道具の違う 2 回に分けず、06 の計測（g2・長いタスク・step の所要時間）と切り分けの URL と一緒に取る。項目 1〜8 は 05 の機能を判定し、PR のスクリーンショットになる |
| R06-9 | ブランチと PR の分け方 | (b) マイルストーンごとにブランチを積み重ね、それぞれ PR にする | マイルストーンごとに PR を分ければ、レビューの単位を小さく保てる（裁定は本節冒頭を参照） |
| R06-10 | 性能以外の「06 で扱う」とされた項目 | (b) ポップオーバーの Popper 化と `awaitingStyle` の復帰は 06 の最後のマイルストーン、上流の issue は下書き、残りは後回し | 性能に関わる後回しの中でも優先度が高い 2 件を 06 に含めれば、後続の spec を減らせる（裁定は本節冒頭を参照） |
| R06-11 | 矢印の間隔に 5 m を選んだときの fps（M3 の結果を受けた M4 の対応） | (a2) すべての範囲で矢印の 1 辺の本数の上限を 50 にする。UI の形は「5 m の選択肢を外す」（`ARROW_SPACINGS` を `[10, 20]` に。保存済み・計測だけの URL の 5 は 10 へ移行） | 矢印 10,000 本のコスト（約 +9.5 ms、1 フレームの予算の半分超）は範囲によらない。500 m が D15 を保つのは 60 Hz の天井が超過（約 1.2 ms）を吸収しているだけで、余裕があるわけではない（g2 0 → 17、3 回中 1 回は単独で割る）。範囲で線を引く案（1000 m だけ実効間隔の下限を設ける）より、コストの実体（本数）に線を引く方が機体に依存しない。詳細は `.superpowers/sdd/2026-09-17-06-performance/arrow-ruling-request.md` |
