# 実装 spec 群の概要

- Status: Draft（レビュー待ち）
- 日付: 2026-09-10
- 入力: `specs/base-spec.md`（何を作るか）、`specs/tech-spec.md`（何で・どう作るか）

---

## 1. 本書の位置づけ

base-spec と tech-spec を、実装計画（implementation plan）を1本ずつ立てられる大きさに分けた spec 群の索引である。各 spec の範囲、依存関係、PoC 完成条件との対応、そして**ユーザーの最終裁定が必要な論点の一覧**（§6）をまとめる。

## 2. 進め方

1. ブランチ `spec/implementation-specs` で、本書を含む全 spec を作成する
2. レビュー役がレビューし、指摘を反映する
3. ユーザーが §6 の論点を最終裁定し、結果を各 spec と tech-spec に反映する（tech-spec の改訂は §7）
4. `main` へマージする
5. spec ごとにブランチを切り、implementation plan を立てて実装する

S（スパイク）と 05 は、スパイクの結論が出た時点で 05 を改訂し、再レビューを経てから実装する（R05-3）。

## 3. spec 一覧

| ID | 名前 | 範囲の要約 | 依存 | base-spec のフェーズ |
|---|---|---|---|---|
| 01 | 基盤（walking skeleton） | 雛形、品質ゲート、CI、Cloudflare へのデプロイ、地図を表示するだけの画面 | なし | Phase 1 の前提 |
| 02 | DEM グリッドと 2D 表示 | 地点選択、DEM の取得・選択・デコード、地形解析（最低点・流向・窪地・spill point）、2D 表示 | 01 | Phase 1 |
| 03 | シミュレーションエンジン | 純粋 TS の水移動エンジン、降雨の投入、質量保存、越流イベント | 01（越流イベントのみ 02 の地形解析） | Phase 2〜3 |
| 04 | 降雨と UI の統合 | Worker との接続、再生制御、統計、セル情報、水深の 2D 表示、設定の保存、免責表示 | 02、03 | Phase 4 |
| S | 3D 描画スパイク | 地形・水面の 3D 描画方式の比較（使い捨て） | 01（02 があれば実データを使う） | — |
| 05 | 3D 描画 | スパイクの結論に基づく 3D の地形・水面・流向 | S、04 | base-spec §28〜§32 |
| 06 | 性能 | 計測と、条件付き最適化の判断 | 04、05 | Phase 5 |

### 依存関係

```
01 ─┬─→ 02 ─┬───────────→ 04 ─→ 05 ─→ 06
    │       └─(地形解析)─→ 03 ─┘     ↑
    ├─────────────────────→ 03       │
    └─────────────────────→ S ───────┘
```

- 02 と 03 は並行して進められる。03 のうち越流イベント（R03-6）だけが、02 の地形解析を必要とする
- S は 01 の後ならいつでも始められる。02 が済んでいれば実データで検証できる

## 4. PoC 完成条件（base-spec §57）との対応

| # | 完成条件 | 担当 |
|---|---|---|
| 1 | 日本国内の地図を表示できる | 01 |
| 2 | 任意地点をクリックできる | 02 |
| 3 | 周囲 500m の DEM を取得できる | 02 |
| 4 | 3D 地形を表示できる | 05 |
| 5 | 指定地点・指定半径に水を追加できる | 03（エンジン）、04（UI） |
| 6 | 水が高所から低所へ移動する | 03 |
| 7 | 窪地に水が溜まる | 03 |
| 8 | 水位上昇によって越流する | 03 |
| 9 | 水深を 3D 表示できる | 05 |
| 10 | 総水量が保存される | 03 |
| 11 | 領域外流出量を計算できる | 03（計算）、04（表示） |
| 12 | Reset できる | 04 |

04 の完了時点で、3D 以外（#4・#9 以外）はすべて 2D で確認できる。

## 5. tech-spec §20（実装 spec へ引き継ぐ事項）の行き先

| tech-spec §20 | 事項 | 決める spec | 論点 |
|---|---|---|---|
| #1 | 地図クリックの意味 | 04 | R04-1 |
| #2 | Phase 1 の範囲 | 02 | R02-1 |
| #3 | 流れのモデルの詳細 | 03 | R03-1、R03-2、R03-3、R03-5 |
| #4 | DEM のカバレッジの混在 | 02 | R02-2 |
| #5 | 粗い DEM での降雨量 | 03 | R03-4 |
| #6 | 3 操作の目標と免責表示 | 04 | R04-4 |
| #7 | 越流イベント | 03 | R03-6 |

## 6. 裁定一覧

「推奨」は各 spec の作成者（実装役）の案である。「レビュー」欄はレビュー役の見解、「裁定」欄はユーザーの最終判断を記入する。詳細と理由は各 spec の「裁定が必要な論点」節を参照。

| ID | 論点 | 推奨 | レビュー | 裁定 |
|---|---|---|---|---|
| R01-1 | ライセンスとリポジトリの公開・非公開 | 公開、MIT | | |
| R01-2 | Renovate を 01 で導入するか | 導入する（GitHub App のインストールはユーザー操作） | | |
| R01-3 | デプロイ先と方式 | `workers.dev` のサブドメイン。`main` への push で本番、PR ごとにプレビュー | | |
| R01-4 | CI の E2E で使うブラウザ | Chromium のみ。Firefox と Safari は各 spec の完了時に手動確認 | | |
| R01-5 | 地図の初期表示 | 日本全体（北緯 36.0°・東経 138.0°、ズーム 5） | | |
| R01-6 | 最小の Worker（ping）を 01 に含めるか | 含める | | |
| R01-7 | CSP（Content-Security-Policy）を 01 で入れるか | 入れる | | |
| R02-1 | Phase 1 の範囲 | 2D 表示までとし、3D は S と 05 に移す（base-spec §55 との差異） | | |
| R02-2 | DEM の選び方 | 3 段（DEM1A → DEM5 → DEM10B）。DEM5 はタイル単位で 5A→5B→5C を合成。404 のタイルは `dem_png` で海域か判定 | | |
| R02-3 | 窪地の表示閾値 | 最大深さ 5cm 以上、かつ面積 10m² 以上 | | |
| R02-4 | 範囲の大きさ | 02 では 500m 固定。250m・1000m の選択 UI は 04 | | |
| R02-5 | OffscreenCanvas の扱い | 必須とし、メインスレッドでデコードする経路は作らない | | |
| R02-6 | 地形解析の置き場所 | `src/simulation/terrain/` に純粋関数として置く | | |
| R02-7 | 手動確認に使う 3 地点 | ユーザーが指定（未指定なら平坦な市街地・谷のある斜面・沿岸を実装役が選ぶ） | | |
| R03-1 | 流量の式 | 陽的な比例配分 `f = k·w·ΔH`（`k·Σw = 0.5`、対角の重み 1/√2、水量で上限） | | |
| R03-2 | 境界と無効セルの扱い | 仮想の近傍セルの標高を流出元セルの標高と同じにする | | |
| R03-3 | 流れの閾値 θ | 1e-5 m（レビュー役の案 1mm は採らない） | | |
| R03-4 | 降雨量の正規化 | 投入総量を πr² × 雨量に厳密に一致させる | | |
| R03-5 | 静的な平衡計算 | 別アルゴリズムを持たず、動的モデルを収束まで回して得る | | |
| R03-6 | 越流イベントの判定 | 窪地の最低点の水位が spill 標高 − 1cm に達したら、窪地ごとに1回通知 | | |
| R03-7 | エンジンのインターフェース追加 | `setDepressions()`・`flowVectors()`、`StepStats` に `events`・`settled`・`massError` | | |
| R04-1 | 地図クリックの意味 | 地点が未選択ならクリックで設定。選択後はセル情報を開き、その中に「ここを降雨中心にする」 | | |
| R04-2 | 範囲と降雨中心の関係 | 範囲は降雨中心を中心とする。中心を動かすと DEM を再取得してリセット | | |
| R04-3 | 降雨時間 | PoC では瞬時の投入のみ（base-spec §55 Phase 4 の「降雨時間」は後回し） | | |
| R04-4 | 3 操作の数え方 | 初回の免責の了解を除いて数える | | |
| R04-5 | 再生速度と自動停止 | 1x = 毎秒 60 step。0.25x〜4x。平衡に達したら自動で一時停止 | | |
| R04-6 | 入力の範囲 | 雨量 1〜1000mm（整数）、半径 1m〜範囲の半分 | | |
| R04-7 | 水深の 2D 表示の方式 | メインスレッドの canvas で着色（GPU 化は 05・06） | | |
| RS-1 | スパイクの期間 | 3 日 | | |
| RS-2 | 生の WebGL2 案も比較するか | 比較する（Three.js が本当に要るかを確かめる） | | |
| R05-1 | 2D 表示の扱い | 3D と切り替えられる常設のモードとして残す | | |
| R05-2 | 水面の描画方式 | 標高と水深をテクスチャで GPU に渡し、頂点シェーダで高さを付ける | | |
| R05-3 | 05 の確定の仕方 | S の結論を受けて改訂し、再レビューしてから実装する | | |
| R06-1 | ベンチマークの地点 | R02-7 と同じ 3 地点 | | |
| R06-2 | 性能計測を CI の必須ゲートにするか | しない（ローカル計測と記録。傾向を見る任意のジョブは可） | | |
| R06-3 | 平衡までの時間の目標 | 1x で、半径 10m は 60 秒以内、半径 100m は 5 分以内 | | |

## 7. tech-spec の改訂（裁定後に本ブランチで反映する）

| # | tech-spec | 内容 | 由来 |
|---|---|---|---|
| T1 | §4.1 | `src/simulation/terrain/`（D8 流向・窪地解析）を追加 | R02-6 |
| T2 | §6.2 | `SimulationEngine` に `setDepressions()`・`flowVectors()`、`StepStats` に `events`・`settled`・`massError` を追加 | R03-7 |
| T3 | §6.6 | 流れの閾値 θ = 1e-5 m を許容誤差の表に追加 | R03-3 |
| T4 | §7.4 | DEM5 のタイル単位合成と、`dem_png` による海域判定を追加 | R02-2 |
| T5 | §7.5、§15.2 | OffscreenCanvas を必須にし、フォールバック経路を削除 | R02-5 |
| T6 | §14.2 | 01 の実測に基づくチャンク別の予算 | 01 の完了時 |
| T7 | §16.3、§17 | ライセンスと公開・非公開 | R01-1 |
| T8 | 新設 | CSP の方針 | R01-7 |
| T9 | §5.5 | 3D 描画方式の決定 | S の完了時 |

T6 と T9 は実装の結果を受けて改訂するため、本ブランチでは扱わない。

## 8. base-spec との差異

各 spec が base-spec の記述から外れる箇所。裁定で承認された場合、tech-spec §19 に追記する。

| base-spec | 差異 | 論点 |
|---|---|---|
| §55 Phase 1 の「3D terrain」 | Phase 1 は 2D 表示までとし、3D は S・05 に移す | R02-1 |
| §55 Phase 2 の「水の平衡計算」 | 別アルゴリズムを持たず、動的モデルを収束まで回して得る | R03-5 |
| §55 Phase 4 の「降雨時間」 | PoC では瞬時の投入のみ | R04-3 |
| §16 の「threshold」 | 値を 1e-5 m と定める | R03-3 |
