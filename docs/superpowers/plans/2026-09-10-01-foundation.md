# Spec 01 基盤（walking skeleton）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 国土地理院の淡色地図を表示するだけの画面を、すべての品質ゲートを通したうえで Cloudflare Workers から配信できる状態にする。

**Architecture:** Vite 8 + React 19 + MUI 9 の SPA を、`@cloudflare/vite-plugin` で Cloudflare Workers の Static Assets として配信する。純粋な層（`src/simulation`・`src/dem`・`src/shared`）は tsconfig の分割と dependency-cruiser で隔離する。MapLibre は React の外で命令的に扱い、Worker は ping/pong だけを持つ。

**Tech Stack:** pnpm 12、Node 24、TypeScript 6.0、Vite 8（rolldown）、React 19、MUI 9、MapLibre GL JS 6、Biome 2、dependency-cruiser 18、Vitest 4、Playwright 1.62、wrangler 4、GitHub Actions

**Spec:** `docs/superpowers/specs/2026-09-10-01-foundation-design.md`（あわせて `specs/tech-spec.md`、裁定は `docs/superpowers/specs/2026-09-10-00-overview.md` §6）

## Global Constraints

- Node: `.nvmrc` は `24`、`package.json` の `engines.node` は `>=24 <25`
- pnpm: `packageManager` は `pnpm@12.1.0`（2026-08-29 公開。10 日を過ぎた 12 系の最新）
- 依存は **正確な版で固定する**（`pnpm add -E`）。すべて公開から 10 日を過ぎた版（2026-09-10 に確認）:

  | パッケージ | 版 | 区分 |
  |---|---|---|
  | react / react-dom | 19.2.8 | dependencies |
  | @mui/material | 9.4.0 | dependencies |
  | @emotion/react | 11.14.0 | dependencies |
  | @emotion/styled | 11.14.1 | dependencies |
  | maplibre-gl | 6.6.0 | dependencies |
  | typescript | 6.0.3 | devDependencies |
  | vite | 8.2.2 | devDependencies |
  | @vitejs/plugin-react | 6.1.1 | devDependencies |
  | @cloudflare/vite-plugin | 1.54.2 | devDependencies |
  | wrangler | 4.127.1 | devDependencies |
  | @biomejs/biome | 2.5.11 | devDependencies |
  | lefthook | 2.1.12 | devDependencies |
  | vitest / @vitest/coverage-v8 | 4.1.11 | devDependencies |
  | @playwright/test | 1.62.1 | devDependencies |
  | dependency-cruiser | 18.2.0 | devDependencies |
  | @types/react | 19.2.18 | devDependencies |
  | @types/react-dom | 19.2.5 | devDependencies |
  | @types/node | 24.13.3 | devDependencies |

- **TypeScript は 6.0.3 を使い、7 系は使わない。** typescript 7.0.2 のパッケージは従来の JS API を公開しておらず（`exports` は `version.cjs` と `./unstable/sync` のみ）、dependency-cruiser 18.2.0 は `typescript >=2.0.0 <7.0.0` しか扱えない（TS 7 では 0 モジュールしか辿れないことを 2026-09-10 に確認）
- GitHub Actions は SHA でピン留めする（すべて公開から 10 日以上）:
  - `actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1` # v7.0.1
  - `actions/setup-node@820762786026740c76f36085b0efc47a31fe5020` # v7.0.0
  - `pnpm/action-setup@0977fd99725f1db4007ccb2928dbb4e90d06cc86` # v6.0.10
- 画面に出す文字列は `src/ui/strings.ts` にだけ置く（tech-spec §9.4）
- コードの書式は Biome に従う（2 スペース、シングルクォート、セミコロンなし、行幅 100）
- `src/simulation/`・`src/dem/`・`src/shared/` の相対 import には `.ts` の拡張子を付ける。それ以外には付けない
- コミットメッセージは日本語。末尾に `Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>` を付ける
- push と PR の作成はユーザーが行う。実装役はコミットまでを行い、`.handoff/`（git 管理外）に手順と PR 本文を残す

## 事前に確かめた事実（2026-09-10、使い捨ての実験環境で確認）

| 事実 | 計画への影響 |
|---|---|
| pnpm 12 は、許可されていないビルドスクリプトがあると `ERR_PNPM_IGNORED_BUILDS` で install を失敗させる（esbuild、workerd） | `allowBuilds` に `false` で明記する。`false` のままで build と `vite preview`（workerd）が動くことを確認済み |
| `@cloudflare/vite-plugin` 使用時、`vite preview` の応答に `public/_headers` の CSP が付く。`vite dev` には付かない | R01-8 は導入で確定。CSP の二重管理（`csp.config.ts`）は不要。dev では React Refresh のインラインスクリプトが阻まれない |
| プラグインはビルド時に `dist/wrangler.json` を出力し、`wrangler deploy` はそれを読む。環境はビルド時の `CLOUDFLARE_ENV` で決まる | ステージングは `CLOUDFLARE_ENV=staging pnpm build` → `wrangler deploy`（spec の `--env staging` の代わり。PR に差異として記録） |
| `wrangler versions upload --preview-alias <名前>` でプレビュー URL を発行できる | プレビューは `raintrace-staging` の Worker に上げる（本番の Worker に触れない。PR に記録） |
| MapLibre 6 は ESM のみで default export が無い。内部 Worker の URL を実行時に `import.meta.url` から組み立てるため、バンドル後は存在しない URL になる | `import workerUrl from 'maplibre-gl/dist/maplibre-gl-worker.mjs?worker&url'` と `setWorkerUrl(workerUrl)`。spec の CSP の下、ヘッドレス Chromium で地図の load、タイル取得、CSP 違反 0 件を確認済み |
| Vite 8 では `build.rollupOptions` と `output.manualChunks` が非推奨。`build.rolldownOptions.output.codeSplitting.groups` を使う | §4.9 のチャンク分けはこれで行う |
| Biome の `useImportExtensions` は `overrides` の中で `error` にしても発火しない。トップレベルで `error` にし、override で `off` にする形なら効く（否定形の glob も使える） | biome.json は「全体で error、純粋な 3 層以外で off」 |
| dependency-cruiser は `new Worker(new URL(...))` を依存として辿らない | `no-orphans` から `src/workers/*.worker.ts` を除外する |
| TS 6.0.3 で tsconfig を分割すると、`src/ui` の `arr[0] + 1` は TS2532、`src/dem` の `document` と `src/simulation` の `console` は TS2584 で失敗する | spec §4.5 の構成がそのまま成り立つ |
| `packageManager: pnpm@12.1.0` にすると、手元の pnpm 12.2.1 は自動で 12.1.0 に切り替わる | 手元でも CI と同じ版で動く |

## ファイル構成

| ファイル | 責務 | Task |
|---|---|---|
| `package.json`、`pnpm-workspace.yaml`、`.npmrc`、`.nvmrc`、`.gitignore`、`LICENSE`、`README.md`、`biome.json` | 雛形、クールダウン、書式と lint | 1 |
| `tsconfig.base.json` | 全プロジェクト共通の厳格化フラグ（tech-spec §10.1）。spec の一覧に無い追加ファイル | 2 |
| `tsconfig.json`、`tsconfig.sim.json`、`tsconfig.core.json`、`tsconfig.test.json`、`tsconfig.node.json` | project references による隔離 | 2 |
| `vitest.config.ts` | ユニットテストとカバレッジ閾値 | 2 |
| `src/simulation/constants.ts`（+ test） | tech-spec §6.6 の許容誤差 | 2 |
| `src/dem/tileMath.ts`（+ test） | 緯度経度・グローバルピクセル・タイルの変換、地上解像度 | 2 |
| `src/shared/protocol.ts` | Worker とのメッセージ型 | 2 |
| `tsconfig.app.json`、`vite.config.ts`、`wrangler.jsonc`、`index.html` | アプリのビルドと配信 | 3 |
| `src/main.tsx` | 対応機能の検出、React の起動、Worker の疎通確認 | 3、5 |
| `src/ui/App.tsx`、`src/ui/theme.ts`、`src/ui/strings.ts`、`src/ui/components/WebGLUnsupported.tsx` | 画面の骨格 | 3 |
| `playwright.config.ts`、`tests/e2e/smoke.spec.ts` | E2E | 3〜5 |
| `src/map/gsiStyle.ts`、`src/map/MapController.ts`、`src/ui/components/MapView.tsx` | 地図 | 4 |
| `public/_headers`、`tests/e2e/fixtures/tile.png` | CSP、E2E 用のタイル | 4 |
| `tsconfig.worker.json`、`src/workers/simulation.worker.ts`、`src/bridge/SimulationClient.ts`（+ test） | Worker の骨組み | 5 |
| `.dependency-cruiser.mjs`、`lefthook.yml` | 依存規則とローカルのフック | 6 |
| `scripts/lib/bundleBudget.mjs`（+ test）、`scripts/check-bundle-size.mjs` | バンドル予算 | 7 |
| `scripts/lib/licensePolicy.mjs`（+ test）、`scripts/check-licenses.mjs` | ライセンス検査 | 7 |
| `.github/actions/setup/action.yml`、`.github/workflows/ci.yml` | CI/CD。セットアップの手順を 6 ジョブで共有するため、ローカルの composite action を追加する | 8 |
| `specs/tech-spec.md`、`.handoff/*` | 実測の反映と引き継ぎ | 9 |

`scripts/lib/` は、判定のロジックを I/O から分けてテストするための追加である（spec の一覧には無い）。

---

### Task 1: 雛形とパッケージマネージャの設定

**Files:**
- Create: `package.json`、`pnpm-workspace.yaml`、`.npmrc`、`.nvmrc`、`.gitignore`、`LICENSE`、`README.md`、`biome.json`

**Interfaces:**
- Produces: `pnpm lint`（`biome ci`）、`pnpm format`。以降のタスクは `pnpm add -E` で依存を足す

- [ ] **Step 1: 設定ファイルを作る**

`package.json`:

```json
{
  "name": "raintrace",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "license": "MIT",
  "packageManager": "pnpm@12.1.0",
  "engines": {
    "node": ">=24 <25"
  },
  "scripts": {
    "lint": "biome ci",
    "format": "biome check --write"
  }
}
```

`pnpm-workspace.yaml`:

```yaml
# pnpm の設定（workspace としては使わない。tech-spec §13.2）
minimumReleaseAge: 14400
minimumReleaseAgeStrict: true
# ビルドスクリプトの実行を許すパッケージ。追加するときは理由を PR に書く（tech-spec §13.3）
allowBuilds: {}
```

`.npmrc`:

```
registry=https://registry.npmjs.org/
```

`.nvmrc`:

```
24
```

`.gitignore`:

```
node_modules/
dist/
coverage/
.wrangler/
test-results/
playwright-report/
.dev.vars*
.handoff/
*.log
```

`LICENSE`: MIT ライセンスの標準の本文。著作権行は `Copyright (c) 2026 Manabu TERADA`。

`README.md`:

````markdown
# raintrace

国土地理院の標高データ（DEM）を使い、雨水が地表を流れて窪地に溜まる様子をブラウザで確かめる、局所雨水流動・湛水シミュレータです。

- 基本仕様: [specs/base-spec.md](specs/base-spec.md)
- 技術仕様: [specs/tech-spec.md](specs/tech-spec.md)
- 実装 spec: [docs/superpowers/specs/](docs/superpowers/specs/)

## 開発

Node 24 と pnpm 12（`package.json` の `packageManager` の版）が必要です。

```sh
pnpm install
pnpm dev
```

| コマンド | 内容 |
|---|---|
| `pnpm dev` | 開発サーバー |
| `pnpm build` | 型検査と本番ビルド |
| `pnpm preview` | 本番ビルドの配信（Cloudflare の workerd） |
| `pnpm lint` | Biome |
| `pnpm typecheck` | `tsc -b` |
| `pnpm depcheck` | dependency-cruiser によるレイヤー規則の検査 |
| `pnpm test` | ユニットテスト |
| `pnpm test:e2e` | 本番ビルドに対する E2E |

## 出典

地図・標高データ：[国土地理院](https://maps.gsi.go.jp/development/ichiran.html)

## ライセンス

[MIT](LICENSE)
````

`biome.json`:

```json
{
  "$schema": "./node_modules/@biomejs/biome/configuration_schema.json",
  "vcs": { "enabled": true, "clientKind": "git", "useIgnoreFile": true },
  "files": { "includes": ["**", "!docs/**", "!specs/**"] },
  "formatter": { "enabled": true, "indentStyle": "space", "indentWidth": 2, "lineWidth": 100 },
  "javascript": { "formatter": { "quoteStyle": "single", "semicolons": "asNeeded" } },
  "linter": {
    "enabled": true,
    "domains": { "react": "recommended" },
    "rules": {
      "recommended": true,
      "correctness": { "useImportExtensions": "error" }
    }
  },
  "overrides": [
    {
      "includes": ["**", "!src/simulation/**", "!src/dem/**", "!src/shared/**"],
      "linter": { "rules": { "correctness": { "useImportExtensions": "off" } } }
    }
  ]
}
```

`useImportExtensions` をトップレベルで有効にし、純粋な 3 層以外で切るのは、override の中で有効にしても発火しないため（事前に確認した事実）。

- [ ] **Step 2: Biome を入れる**

Run: `pnpm add -DE @biomejs/biome@2.5.11`
Expected: 成功。`pnpm-lock.yaml` ができる。ビルドスクリプトの許可を求められたら、その名前を控えて Task 3 の Step 2 と同じ手順で `allowBuilds` に `false` で足す

- [ ] **Step 3: リポジトリの設定が効いていることを確かめる（spec §7 の完了条件 4）**

Run: `pnpm config get minimumReleaseAge && XDG_CONFIG_HOME=$(mktemp -d) pnpm config get minimumReleaseAge && (cd /tmp && XDG_CONFIG_HOME=$(mktemp -d) pnpm config get minimumReleaseAge)`
Expected: `14400`、`14400`、そして `/tmp` では pnpm の既定値（`1440`）。グローバル設定を外してもリポジトリ内で `14400` なら、`pnpm-workspace.yaml` が効いている。結果を `.handoff/01-foundation.md` の記録欄（Task 9）に控える

- [ ] **Step 4: lint を通す**

Run: `pnpm format && pnpm lint`
Expected: `biome ci` がエラー 0 で終わる

- [ ] **Step 5: Commit**

```bash
git add package.json pnpm-lock.yaml pnpm-workspace.yaml .npmrc .nvmrc .gitignore LICENSE README.md biome.json
git commit -m "雛形: pnpm 12 とクールダウン、Biome、MIT ライセンス"
```

---

### Task 2: 純粋な層と型検査・ユニットテストの構成

**Files:**
- Create: `tsconfig.base.json`、`tsconfig.json`、`tsconfig.sim.json`、`tsconfig.core.json`、`tsconfig.test.json`、`tsconfig.node.json`、`vitest.config.ts`
- Create: `src/simulation/constants.ts`、`src/simulation/constants.test.ts`
- Create: `src/dem/tileMath.ts`、`src/dem/tileMath.test.ts`
- Create: `src/shared/protocol.ts`
- Modify: `package.json`（scripts）

**Interfaces:**
- Produces（02・03 が使う）:
  - `constants.ts`: `MASS_TOLERANCE_REL = 1e-9`、`SURFACE_ELEVATION_TOLERANCE_M = 0.01`、`DEPTH_EPSILON_M = 1e-5`、`FLOW_THRESHOLD_M = 1e-5`、`massTolerance(totalInputM3: number): number`
  - `tileMath.ts`: `TILE_SIZE = 256`、`EARTH_RADIUS_M = 6378137`、`worldSizePx(z: number): number`、`lonLatToPixel(lon: number, lat: number, z: number): PixelPoint`、`pixelToLonLat(x: number, y: number, z: number): LonLat`、`groundResolutionM(lat: number, z: number): number`、`tilesInPixelRect(rect: PixelRect, z: number): TileCoord[]`。型 `PixelPoint { x; y }`、`LonLat { lon; lat }`、`TileCoord { z; x; y }`、`PixelRect { x0; y0; x1; y1 }`（半開区間）
  - `protocol.ts`: `MainToWorkerMessage = { type: 'ping'; id: number }`、`WorkerToMainMessage = { type: 'pong'; id: number }`
  - scripts: `pnpm typecheck`（`tsc -b`）、`pnpm test`、`pnpm test:coverage`

- [ ] **Step 1: 依存を入れる**

Run: `pnpm add -DE typescript@6.0.3 vitest@4.1.11 @vitest/coverage-v8@4.1.11 @types/node@24.13.3`
Expected: 成功

- [ ] **Step 2: tsconfig を作る**

`tsconfig.base.json`（tech-spec §10.1。`types: []` で `@types/*` の自動読み込みを止め、純粋な層に Node の型を持ち込まない）:

```json
{
  "compilerOptions": {
    "target": "ES2023",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "strict": true,
    "noImplicitOverride": true,
    "noFallthroughCasesInSwitch": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "exactOptionalPropertyTypes": true,
    "verbatimModuleSyntax": true,
    "isolatedModules": true,
    "erasableSyntaxOnly": true,
    "moduleDetection": "force",
    "skipLibCheck": true,
    "types": []
  }
}
```

`tsconfig.sim.json`:

```json
{
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    "composite": true,
    "emitDeclarationOnly": true,
    "rootDir": "src",
    "outDir": "node_modules/.tmp/tsc/sim",
    "tsBuildInfoFile": "node_modules/.tmp/tsc/sim.tsbuildinfo",
    "lib": ["ES2023"],
    "noUncheckedIndexedAccess": false,
    "allowImportingTsExtensions": true
  },
  "include": ["src/simulation"],
  "exclude": ["src/**/*.test.ts"]
}
```

`tsconfig.core.json`:

```json
{
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    "composite": true,
    "emitDeclarationOnly": true,
    "rootDir": "src",
    "outDir": "node_modules/.tmp/tsc/core",
    "tsBuildInfoFile": "node_modules/.tmp/tsc/core.tsbuildinfo",
    "lib": ["ES2023"],
    "noUncheckedIndexedAccess": true,
    "allowImportingTsExtensions": true
  },
  "include": ["src/dem", "src/shared"],
  "exclude": ["src/**/*.test.ts"]
}
```

`tsconfig.test.json`（spec §4.5、T10）:

```json
{
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    "noEmit": true,
    "tsBuildInfoFile": "node_modules/.tmp/tsc/test.tsbuildinfo",
    "lib": ["ES2023", "DOM"],
    "types": ["node", "vitest"],
    "noUncheckedIndexedAccess": true,
    "allowImportingTsExtensions": true
  },
  "include": ["src/**/*.test.ts"],
  "references": [{ "path": "./tsconfig.sim.json" }, { "path": "./tsconfig.core.json" }]
}
```

`tsconfig.node.json`（Task 3 で `vite.config.ts` などを足す）:

```json
{
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    "noEmit": true,
    "tsBuildInfoFile": "node_modules/.tmp/tsc/node.tsbuildinfo",
    "lib": ["ES2023"],
    "types": ["node"],
    "noUncheckedIndexedAccess": true
  },
  "include": ["vitest.config.ts"]
}
```

`tsconfig.json`:

```json
{
  "files": [],
  "references": [
    { "path": "./tsconfig.sim.json" },
    { "path": "./tsconfig.core.json" },
    { "path": "./tsconfig.test.json" },
    { "path": "./tsconfig.node.json" }
  ]
}
```

`vitest.config.ts`（tech-spec §11.5。閾値は対象のディレクトリができた spec で足す。`src/state/` は 04）:

```ts
import { defineConfig } from 'vitest/config'

export default defineConfig({
  test: {
    environment: 'node',
    include: ['src/**/*.test.ts'],
    coverage: {
      provider: 'v8',
      include: ['src/**/*.{ts,tsx}'],
      exclude: ['src/**/*.test.ts'],
      thresholds: {
        'src/simulation/**': { lines: 90 },
        'src/dem/**': { lines: 85 },
      },
    },
  },
})
```

`package.json` の scripts に足す:

```json
"typecheck": "tsc -b",
"test": "vitest run",
"test:coverage": "vitest run --coverage"
```

- [ ] **Step 3: 定数の失敗するテストを書く**

`src/simulation/constants.test.ts`:

```ts
import { describe, expect, it } from 'vitest'
import {
  DEPTH_EPSILON_M,
  FLOW_THRESHOLD_M,
  MASS_TOLERANCE_REL,
  massTolerance,
  SURFACE_ELEVATION_TOLERANCE_M,
} from './constants.ts'

describe('許容誤差（tech-spec §6.6）', () => {
  it('表の値と一致する', () => {
    expect(MASS_TOLERANCE_REL).toBe(1e-9)
    expect(SURFACE_ELEVATION_TOLERANCE_M).toBe(0.01)
    expect(FLOW_THRESHOLD_M).toBe(1e-5)
  })

  it('水深の比較許容値は流れの閾値 θ と同じ値', () => {
    expect(DEPTH_EPSILON_M).toBe(FLOW_THRESHOLD_M)
  })

  it('質量保存の許容誤差は投入総量に比例する', () => {
    expect(massTolerance(0)).toBe(0)
    expect(massTolerance(1000)).toBeCloseTo(1e-6, 15)
  })
})
```

Run: `pnpm test`
Expected: FAIL（`./constants.ts` が見つからない）

- [ ] **Step 4: 定数を実装する**

`src/simulation/constants.ts`（名前は spec 03 §3.11 に揃える。03 が `DIFFUSION_C`・`FLOODED_DEPTH_M`・`SPILL_TOLERANCE_M` を足す）:

```ts
/**
 * 許容誤差と閾値（tech-spec §6.6）。値を変えるときは tech-spec も改訂する
 */

/** 質量保存の許容誤差の係数。許容誤差 = (初期水量 + 投入水量の累計) × この値 */
export const MASS_TOLERANCE_REL = 1e-9

/** 平衡状態の水面標高と、体積から求めた理論値との差の許容値（m） */
export const SURFACE_ELEVATION_TOLERANCE_M = 0.01

/** 水深・水面の比較の許容値 epsilon（m）。流れの閾値 θ と同じ値 */
export const DEPTH_EPSILON_M = 1e-5

/** 流れの閾値 θ（m）。水面差がこれ以下の近傍には流さない（R03-3） */
export const FLOW_THRESHOLD_M = 1e-5

/** 質量保存の許容誤差（m³）。totalInputM3 は初期水量と投入水量の累計の和 */
export function massTolerance(totalInputM3: number): number {
  return totalInputM3 * MASS_TOLERANCE_REL
}
```

Run: `pnpm test`
Expected: PASS

- [ ] **Step 5: tileMath の失敗するテストを書く**

`src/dem/tileMath.test.ts`:

```ts
import { describe, expect, it } from 'vitest'
import {
  groundResolutionM,
  lonLatToPixel,
  pixelToLonLat,
  tilesInPixelRect,
  worldSizePx,
} from './tileMath.ts'

describe('lonLatToPixel と pixelToLonLat', () => {
  it('経度 0・緯度 0 は z0 の世界の中心', () => {
    expect(lonLatToPixel(0, 0, 0)).toEqual({ x: 128, y: 128 })
  })

  it('z が 1 増えるとピクセル座標は 2 倍', () => {
    const a = lonLatToPixel(139.7671, 35.6812, 16)
    const b = lonLatToPixel(139.7671, 35.6812, 17)
    expect(b.x).toBeCloseTo(a.x * 2, 6)
    expect(b.y).toBeCloseTo(a.y * 2, 6)
  })

  it.each([
    [139.7671, 35.6812],
    [127.6809, 26.2124],
    [141.6739, 45.4153],
  ])('経度 %f・緯度 %f を往復させても 1e-9 度以内で戻る', (lon, lat) => {
    const p = lonLatToPixel(lon, lat, 17)
    const back = pixelToLonLat(p.x, p.y, 17)
    expect(back.lon).toBeCloseTo(lon, 9)
    expect(back.lat).toBeCloseTo(lat, 9)
  })
})

describe('groundResolutionM', () => {
  it('赤道の z0 は 156543.03m', () => {
    expect(groundResolutionM(0, 0)).toBeCloseTo(156543.03392804097, 6)
  })

  it('北緯 35° の z17 は約 0.978m（誤差 0.001m 以内）', () => {
    expect(Math.abs(groundResolutionM(35, 17) - 0.978)).toBeLessThan(0.001)
  })

  it('世界の幅は 256 × 2^z ピクセル', () => {
    expect(worldSizePx(17)).toBe(256 * 131072)
  })
})

describe('tilesInPixelRect', () => {
  it('範囲にかかるタイルを行優先で列挙する', () => {
    const tiles = tilesInPixelRect({ x0: 2660, y0: 1300, x1: 3172, y1: 1812 }, 17)
    expect(tiles).toHaveLength(9)
    expect(tiles[0]).toEqual({ z: 17, x: 10, y: 5 })
    expect(tiles[1]).toEqual({ z: 17, x: 11, y: 5 })
    expect(tiles[8]).toEqual({ z: 17, x: 12, y: 7 })
  })

  it('タイルの境界ちょうどの範囲は 1 枚', () => {
    expect(tilesInPixelRect({ x0: 256, y0: 256, x1: 512, y1: 512 }, 3)).toEqual([
      { z: 3, x: 1, y: 1 },
    ])
  })

  it('端が小数でも、少しでもかかるタイルは含める', () => {
    expect(tilesInPixelRect({ x0: 0, y0: 0, x1: 256.5, y1: 10 }, 3)).toEqual([
      { z: 3, x: 0, y: 0 },
      { z: 3, x: 1, y: 0 },
    ])
  })

  it('空の範囲は空の配列', () => {
    expect(tilesInPixelRect({ x0: 10, y0: 0, x1: 10, y1: 10 }, 3)).toEqual([])
  })
})
```

Run: `pnpm test`
Expected: FAIL（`./tileMath.ts` が見つからない）

- [ ] **Step 6: tileMath を実装する**

`src/dem/tileMath.ts`:

```ts
/**
 * 地理院タイル（Web メルカトル）の座標計算（tech-spec §7.6）。
 * ピクセル座標は、ズーム z の世界全体を 256 × 2^z ピクセルとしたときの座標（グローバルピクセル）
 */

export const TILE_SIZE = 256
export const EARTH_RADIUS_M = 6378137

export interface PixelPoint {
  x: number
  y: number
}

export interface LonLat {
  lon: number
  lat: number
}

export interface TileCoord {
  z: number
  x: number
  y: number
}

/** グローバルピクセルの矩形。x1・y1 を含まない半開区間 */
export interface PixelRect {
  x0: number
  y0: number
  x1: number
  y1: number
}

export function worldSizePx(z: number): number {
  return TILE_SIZE * 2 ** z
}

export function lonLatToPixel(lon: number, lat: number, z: number): PixelPoint {
  const size = worldSizePx(z)
  const phi = (lat * Math.PI) / 180
  return {
    x: ((lon + 180) / 360) * size,
    y: (0.5 - Math.log(Math.tan(Math.PI / 4 + phi / 2)) / (2 * Math.PI)) * size,
  }
}

export function pixelToLonLat(x: number, y: number, z: number): LonLat {
  const size = worldSizePx(z)
  const n = Math.PI - (2 * Math.PI * y) / size
  return {
    lon: (x / size) * 360 - 180,
    lat: (Math.atan(Math.sinh(n)) * 180) / Math.PI,
  }
}

/** 地上解像度（m／ピクセル）。cellSizeM = 2π × 6378137 × cos(φ) / (256 × 2^z) */
export function groundResolutionM(lat: number, z: number): number {
  return (2 * Math.PI * EARTH_RADIUS_M * Math.cos((lat * Math.PI) / 180)) / worldSizePx(z)
}

/** 矩形に少しでもかかるタイルを、行優先（北から、西から）で返す */
export function tilesInPixelRect(rect: PixelRect, z: number): TileCoord[] {
  if (rect.x1 <= rect.x0 || rect.y1 <= rect.y0) return []
  const tx0 = Math.floor(rect.x0 / TILE_SIZE)
  const ty0 = Math.floor(rect.y0 / TILE_SIZE)
  const tx1 = Math.ceil(rect.x1 / TILE_SIZE) - 1
  const ty1 = Math.ceil(rect.y1 / TILE_SIZE) - 1
  const tiles: TileCoord[] = []
  for (let y = ty0; y <= ty1; y++) {
    for (let x = tx0; x <= tx1; x++) tiles.push({ z, x, y })
  }
  return tiles
}
```

Run: `pnpm test`
Expected: PASS（constants と tileMath のすべて）

- [ ] **Step 7: メッセージ型を置く**

`src/shared/protocol.ts`:

```ts
/**
 * メインスレッドと Worker の間のメッセージ（tech-spec §5）。判別可能な union 型で、02 以降で種類を足す
 */

export type MainToWorkerMessage = { type: 'ping'; id: number }

export type WorkerToMainMessage = { type: 'pong'; id: number }
```

- [ ] **Step 8: 型検査、カバレッジ、lint を通す**

Run: `pnpm typecheck && pnpm test:coverage && pnpm lint`
Expected: すべて成功。`node_modules/.tmp/tsc/sim/simulation/constants.d.ts` と `node_modules/.tmp/tsc/core/dem/tileMath.d.ts` ができている。カバレッジは `src/simulation` と `src/dem` が 100% 近い

- [ ] **Step 9: Commit**

```bash
git add tsconfig*.json vitest.config.ts src package.json pnpm-lock.yaml
git commit -m "純粋な層: tsconfig の分割、許容誤差の定数、タイル座標の計算、メッセージ型"
```

---

### Task 3: アプリの骨格（Vite・React・MUI・Cloudflare）と E2E の土台

**Files:**
- Create: `tsconfig.app.json`、`vite.config.ts`、`wrangler.jsonc`、`index.html`、`playwright.config.ts`
- Create: `src/main.tsx`、`src/ui/App.tsx`、`src/ui/theme.ts`、`src/ui/strings.ts`、`src/ui/components/WebGLUnsupported.tsx`
- Create: `tests/e2e/smoke.spec.ts`（E2E の 5）
- Modify: `tsconfig.json`、`tsconfig.node.json`、`pnpm-workspace.yaml`、`package.json`

**Interfaces:**
- Consumes: Task 2 の tsconfig 群
- Produces:
  - `App({ missingFeatures }: { missingFeatures: readonly MissingFeature[] })`
  - `type MissingFeature = 'webgl2' | 'offscreenCanvas'`（`src/ui/components/WebGLUnsupported.tsx`）
  - `strings`（`src/ui/strings.ts`）: `strings.unsupported.title` など。Task 4 で `strings.attribution`・`strings.map` を足す
  - scripts: `pnpm dev`、`pnpm build`、`pnpm preview`、`pnpm test:e2e`

- [ ] **Step 1: 依存を入れる**

Run:

```bash
pnpm add -E react@19.2.8 react-dom@19.2.8 @mui/material@9.4.0 @emotion/react@11.14.0 @emotion/styled@11.14.1
pnpm add -DE vite@8.2.2 @vitejs/plugin-react@6.1.1 @cloudflare/vite-plugin@1.54.2 wrangler@4.127.1 @types/react@19.2.18 @types/react-dom@19.2.5 @playwright/test@1.62.1
```

Expected: 2 つ目のコマンドが `ERR_PNPM_IGNORED_BUILDS`（`Ignored build scripts: esbuild@…, workerd@…`）で失敗する

- [ ] **Step 2: allowBuilds を決める**

`pnpm-workspace.yaml` の `allowBuilds` を、Step 1 で名前が出たパッケージを `false` にしたものに置き換える（想定は次の 2 つ）:

```yaml
# ビルドスクリプトの実行を許すパッケージ。追加するときは理由を PR に書く（tech-spec §13.3）
# false: スクリプトを実行しない。どちらも実行ファイルをプラットフォーム別の optionalDependencies で
# 受け取り、postinstall は検証と最適化だけを行うため、実行しなくても build と preview が動く
allowBuilds:
  esbuild: false
  workerd: false
```

Run: `pnpm install`
Expected: 成功。ほかのパッケージの名前が出た場合は、同じく `false` で足し、Step 9 の確認で動くことを確かめる。`true` が必要なものが出たら、理由を調べて `.handoff/01-foundation.md` の記録欄に書く

- [ ] **Step 3: ビルドと配信の設定を作る**

`vite.config.ts`（Task 7 でチャンク分けを足す）:

```ts
import { cloudflare } from '@cloudflare/vite-plugin'
import react from '@vitejs/plugin-react'
import { defineConfig } from 'vite'

export default defineConfig({
  plugins: [react(), cloudflare()],
  // Worker は { type: 'module' } で起動するので ES モジュールとして出力する
  worker: { format: 'es' },
  build: { manifest: true },
})
```

`wrangler.jsonc`（静的配信のみ。`assets.directory` はプラグインが決めるので書かない）:

```jsonc
{
  "$schema": "./node_modules/wrangler/config-schema.json",
  "name": "raintrace",
  // 実装した日（spec 01 §4.11）
  "compatibility_date": "2026-09-10",
  "workers_dev": true,
  "preview_urls": true,
  "assets": {
    "not_found_handling": "single-page-application"
  },
  "env": {
    // ステージング（R01-3）。ビルド時に CLOUDFLARE_ENV=staging を渡すと、この名前の Worker 向けに出力される
    "staging": {
      "name": "raintrace-staging"
    }
  }
}
```

`index.html`:

```html
<!doctype html>
<html lang="ja">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="color-scheme" content="light dark" />
    <title>raintrace</title>
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.tsx"></script>
  </body>
</html>
```

`tsconfig.app.json`:

```json
{
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    "noEmit": true,
    "tsBuildInfoFile": "node_modules/.tmp/tsc/app.tsbuildinfo",
    "lib": ["ES2023", "DOM", "DOM.Iterable"],
    "types": ["vite/client"],
    "jsx": "react-jsx",
    "noUncheckedIndexedAccess": true
  },
  "include": ["src"],
  "exclude": ["src/simulation", "src/dem", "src/shared", "src/workers", "src/**/*.test.ts"],
  "references": [{ "path": "./tsconfig.sim.json" }, { "path": "./tsconfig.core.json" }]
}
```

`tsconfig.node.json` の `include` を置き換え、DOM を足す（E2E の `page.evaluate` の中で `window` などを参照するため）:

```json
"lib": ["ES2023", "DOM"],
```

```json
"include": ["vite.config.ts", "vitest.config.ts", "playwright.config.ts", "tests/e2e/**/*.ts"]
```

`tsconfig.json` の `references` に `{ "path": "./tsconfig.app.json" }` を足す。

`package.json` の scripts に足す:

```json
"dev": "vite",
"build": "tsc -b && vite build",
"preview": "vite preview",
"test:e2e": "pnpm build && playwright test"
```

- [ ] **Step 4: 画面の骨格を作る**

`src/ui/strings.ts`:

```ts
/** 画面に出す文字列（tech-spec §9.4）。コンポーネントに文字列リテラルを直接書かない */
export const strings = {
  unsupported: {
    title: 'このブラウザには対応していません',
    body: 'raintrace には WebGL 2 と OffscreenCanvas が必要です。Chrome、Edge、Firefox、Safari の最新版でお試しください。',
    missing: {
      webgl2: 'WebGL 2 が使えません',
      offscreenCanvas: 'OffscreenCanvas が使えません',
    },
  },
} as const
```

`src/ui/theme.ts`（フォントは外部から読まずシステムフォントを使う。spec §4.3）:

```ts
import { createTheme } from '@mui/material/styles'

const systemFontFamily = [
  'system-ui',
  '-apple-system',
  'BlinkMacSystemFont',
  '"Segoe UI"',
  '"Hiragino Sans"',
  '"Hiragino Kaku Gothic ProN"',
  '"Noto Sans JP"',
  'Meiryo',
  'sans-serif',
].join(',')

export const theme = createTheme({
  colorSchemes: { light: true, dark: true },
  cssVariables: true,
  typography: { fontFamily: systemFontFamily },
})
```

`src/ui/components/WebGLUnsupported.tsx`:

```tsx
import Alert from '@mui/material/Alert'
import AlertTitle from '@mui/material/AlertTitle'
import Box from '@mui/material/Box'
import { strings } from '../strings'

export type MissingFeature = 'webgl2' | 'offscreenCanvas'

interface Props {
  missing: readonly MissingFeature[]
}

/** 必須の機能（WebGL 2・OffscreenCanvas）が使えないブラウザへの案内（tech-spec §15.2） */
export function WebGLUnsupported({ missing }: Props) {
  return (
    <Box sx={{ display: 'grid', placeItems: 'center', minHeight: '100dvh', p: 2 }}>
      <Alert severity="error" data-testid="unsupported">
        <AlertTitle>{strings.unsupported.title}</AlertTitle>
        {strings.unsupported.body}
        <ul>
          {missing.map((feature) => (
            <li key={feature}>{strings.unsupported.missing[feature]}</li>
          ))}
        </ul>
      </Alert>
    </Box>
  )
}
```

`src/ui/App.tsx`（Task 4 で `Box` を `MapView` に置き換える）:

```tsx
import Box from '@mui/material/Box'
import CssBaseline from '@mui/material/CssBaseline'
import { ThemeProvider } from '@mui/material/styles'
import { type MissingFeature, WebGLUnsupported } from './components/WebGLUnsupported'
import { theme } from './theme'

interface Props {
  missingFeatures: readonly MissingFeature[]
}

export function App({ missingFeatures }: Props) {
  return (
    <ThemeProvider theme={theme} defaultMode="system">
      <CssBaseline />
      {missingFeatures.length > 0 ? (
        <WebGLUnsupported missing={missingFeatures} />
      ) : (
        <Box component="main" sx={{ position: 'fixed', inset: 0 }} />
      )}
    </ThemeProvider>
  )
}
```

`src/main.tsx`:

```tsx
import { StrictMode } from 'react'
import { createRoot } from 'react-dom/client'
import { App } from './ui/App'
import type { MissingFeature } from './ui/components/WebGLUnsupported'

/** 必須の機能を確かめる（spec 01 §4.3）。確かめに使った WebGL のコンテキストはすぐ解放する */
function detectMissingFeatures(): MissingFeature[] {
  const missing: MissingFeature[] = []
  const gl = document.createElement('canvas').getContext('webgl2')
  if (gl === null) missing.push('webgl2')
  else gl.getExtension('WEBGL_lose_context')?.loseContext()
  if (typeof OffscreenCanvas === 'undefined') missing.push('offscreenCanvas')
  return missing
}

const container = document.getElementById('root')
if (container === null) throw new Error('#root が見つかりません')

createRoot(container).render(
  <StrictMode>
    <App missingFeatures={detectMissingFeatures()} />
  </StrictMode>,
)
```

- [ ] **Step 5: E2E の設定と、失敗するテストを書く**

`playwright.config.ts`（R01-4: Chromium で全件、WebKit は `@webkit` タグのみ）:

```ts
import { defineConfig, devices } from '@playwright/test'

const port = 4173

export default defineConfig({
  testDir: 'tests/e2e',
  forbidOnly: !!process.env.CI,
  reporter: process.env.CI ? [['github'], ['list']] : 'list',
  use: { baseURL: `http://localhost:${port}` },
  // 本番ビルドを vite preview（workerd）で配信する。ビルドは pnpm test:e2e が先に行う
  webServer: {
    command: `pnpm preview --port ${port} --strictPort`,
    url: `http://localhost:${port}`,
    reuseExistingServer: !process.env.CI,
    timeout: 60_000,
  },
  projects: [
    {
      name: 'chromium',
      use: {
        ...devices['Desktop Chrome'],
        // GPU の無い CI でも WebGL を SwiftShader で動かす
        launchOptions: { args: ['--enable-unsafe-swiftshader'] },
      },
    },
    { name: 'webkit', use: { ...devices['Desktop Safari'] }, grep: /@webkit/ },
  ],
})
```

`tests/e2e/smoke.spec.ts`:

```ts
import { expect, test } from '@playwright/test'
import { strings } from '../../src/ui/strings'

test('WebGL 2 が使えないと、非対応の画面が出る', async ({ page }) => {
  await page.addInitScript(() => {
    const original = HTMLCanvasElement.prototype.getContext
    Object.defineProperty(HTMLCanvasElement.prototype, 'getContext', {
      value(this: HTMLCanvasElement, type: string, ...rest: unknown[]) {
        return type === 'webgl2' ? null : Reflect.apply(original, this, [type, ...rest])
      },
    })
  })
  await page.goto('/')
  await expect(page.getByTestId('unsupported')).toBeVisible()
  await expect(page.getByText(strings.unsupported.title)).toBeVisible()
  await expect(page.getByText(strings.unsupported.missing.webgl2)).toBeVisible()
})
```

Run: `pnpm exec playwright install chromium webkit`（手元にブラウザが無い場合のみ時間がかかる）
Run: `pnpm test:e2e`
Expected: PASS（このテストは Step 4 の実装に対して書いたもの。念のため `detectMissingFeatures` の `webgl2` の判定を一時的に外すと FAIL することを確かめ、元に戻す）

- [ ] **Step 6: 通常の起動を確かめる**

Run: `pnpm build && (timeout 30 pnpm preview --port 4174 --strictPort > /dev/null 2>&1 &) ; curl -sS --retry 20 --retry-connrefused --retry-delay 1 http://localhost:4174/ | head -5`
Expected: `index.html` が返る。`compatibility_date` について workerd が「サポートする最新の日付より新しい」と警告して起動を拒む場合は、警告に出た日付に置き換え、その理由を `.handoff/01-foundation.md` の記録欄に書く

Run: `(timeout 30 pnpm dev --port 5174 --strictPort > /dev/null 2>&1 &) ; curl -sS --retry 20 --retry-connrefused --retry-delay 1 http://localhost:5174/ | grep -c react-refresh`
Expected: `1`（dev サーバーが動く）

- [ ] **Step 7: 型検査、ユニット、lint を通す**

Run: `pnpm typecheck && pnpm test && pnpm lint`
Expected: すべて成功

- [ ] **Step 8: Commit**

```bash
git add -A
git status --short   # dist や node_modules が入っていないことを確かめる
git commit -m "アプリの骨格: Vite・React・MUI・Cloudflare の配信と、非対応ブラウザの案内"
```

---

### Task 4: 地図（MapLibre）と CSP

**Files:**
- Create: `src/map/gsiStyle.ts`、`src/map/MapController.ts`、`src/ui/components/MapView.tsx`
- Create: `public/_headers`、`tests/e2e/fixtures/tile.png`
- Modify: `src/ui/App.tsx`、`src/ui/strings.ts`、`tests/e2e/smoke.spec.ts`（E2E の 1・2・3・6）

**Interfaces:**
- Consumes: Task 3 の `App`、`strings`
- Produces:
  - `createGsiPaleStyle(attribution: Attribution): StyleSpecification`、`interface Attribution { text: string; url: string }`、`GSI_PALE_TILE_URL`
  - `class MapController { readonly map: maplibregl.Map; constructor(container: HTMLElement, attribution: Attribution); destroy(): void }`、`INITIAL_VIEW`
  - `MapView({ children? })` と `useMapController(): MapController | null`。地図の `load` 後、コンテナに `data-map-loaded="true"` を付ける
  - `strings.attribution = { text, url }`、`strings.map.ariaLabel`

- [ ] **Step 1: 依存を入れる**

Run: `pnpm add -E maplibre-gl@6.6.0`
Expected: 成功

- [ ] **Step 2: E2E 用のタイルを作る**

地理院のタイルの代わりに返す、256 × 256 の単色 PNG（自作）を生成してコミットする:

```bash
mkdir -p tests/e2e/fixtures
node -e '
const zlib = require("node:zlib"), fs = require("node:fs")
const chunk = (type, data) => {
  const len = Buffer.alloc(4); len.writeUInt32BE(data.length)
  const body = Buffer.concat([Buffer.from(type), data])
  const crc = Buffer.alloc(4); crc.writeUInt32BE(zlib.crc32(body))
  return Buffer.concat([len, body, crc])
}
const w = 256, h = 256
const ihdr = Buffer.alloc(13); ihdr.writeUInt32BE(w, 0); ihdr.writeUInt32BE(h, 4); ihdr[8] = 8; ihdr[9] = 2
const row = Buffer.concat([Buffer.from([0]), Buffer.from(Array.from({ length: w }, () => [0xcc, 0xe0, 0xf0]).flat())])
const png = Buffer.concat([
  Buffer.from([137, 80, 78, 71, 13, 10, 26, 10]),
  chunk("IHDR", ihdr), chunk("IDAT", zlib.deflateSync(Buffer.concat(Array(h).fill(row)))), chunk("IEND", Buffer.alloc(0)),
])
fs.writeFileSync("tests/e2e/fixtures/tile.png", png)
'
file tests/e2e/fixtures/tile.png
```

Expected: `PNG image data, 256 x 256, 8-bit/color RGB, non-interlaced`

- [ ] **Step 3: E2E の失敗するテストを書く**

`tests/e2e/smoke.spec.ts` を次の内容に置き換える（Task 3 のテストを含む）:

```ts
import { readFileSync } from 'node:fs'
import { test as base, expect } from '@playwright/test'
import { strings } from '../../src/ui/strings'

const tilePng = readFileSync(new URL('./fixtures/tile.png', import.meta.url))

// 地理院への通信を自作の単色 PNG に差し替える。Worker から出るリクエストも捕まえるため、
// page ではなく browserContext で差し替える（tech-spec §11.4）
const test = base.extend<{ gsiTiles: { count: number } }>({
  gsiTiles: [
    async ({ context }, use) => {
      const counter = { count: 0 }
      await context.route('https://cyberjapandata.gsi.go.jp/**', async (route) => {
        counter.count++
        await route.fulfill({
          status: 200,
          contentType: 'image/png',
          headers: { 'access-control-allow-origin': '*' },
          body: tilePng,
        })
      })
      await use(counter)
    },
    { auto: true },
  ],
})

const mapLoaded = '[data-map-loaded="true"]'

test('地図の canvas が表示される', async ({ page }) => {
  const errors: string[] = []
  page.on('pageerror', (error) => errors.push(error.message))
  await page.goto('/')
  await expect(page.locator(mapLoaded)).toBeAttached()
  await expect(page.locator('canvas.maplibregl-canvas')).toBeVisible()
  expect(errors).toEqual([])
})

test('地図タイルのリクエストが、差し替えた画像で応答される', async ({ page, gsiTiles }) => {
  await page.goto('/')
  await expect.poll(() => gsiTiles.count).toBeGreaterThan(0)
})

test('出典が表示される', async ({ page }) => {
  await page.goto('/')
  const link = page.getByRole('link', { name: strings.attribution.text })
  await expect(link).toBeVisible()
  await expect(link).toHaveAttribute('href', strings.attribution.url)
})

test('WebGL 2 が使えないと、非対応の画面が出る', async ({ page }) => {
  await page.addInitScript(() => {
    const original = HTMLCanvasElement.prototype.getContext
    Object.defineProperty(HTMLCanvasElement.prototype, 'getContext', {
      value(this: HTMLCanvasElement, type: string, ...rest: unknown[]) {
        return type === 'webgl2' ? null : Reflect.apply(original, this, [type, ...rest])
      },
    })
  })
  await page.goto('/')
  await expect(page.getByTestId('unsupported')).toBeVisible()
  await expect(page.getByText(strings.unsupported.title)).toBeVisible()
  await expect(page.getByText(strings.unsupported.missing.webgl2)).toBeVisible()
})

test('応答に CSP が付き、読み込み中に CSP 違反が起きない', async ({ page, gsiTiles }) => {
  await page.addInitScript(() => {
    const violations: string[] = []
    Object.assign(window, { __cspViolations: violations })
    document.addEventListener('securitypolicyviolation', (event) => {
      violations.push(`${event.violatedDirective} ${event.blockedURI}`)
    })
  })
  const response = await page.goto('/')
  expect(response?.headers()['content-security-policy']).toContain("default-src 'self'")
  await expect(page.locator(mapLoaded)).toBeAttached()
  await expect.poll(() => gsiTiles.count).toBeGreaterThan(0)
  const violations = await page.evaluate(
    () => (window as unknown as { __cspViolations: string[] }).__cspViolations,
  )
  expect(violations).toEqual([])
})
```

`src/ui/strings.ts` に、テストが参照する文字列を先に足す（`unsupported` の前に置く）:

```ts
  attribution: {
    text: '地図・標高データ：国土地理院',
    url: 'https://maps.gsi.go.jp/development/ichiran.html',
  },
  map: {
    ariaLabel: '地図',
  },
```

Run: `pnpm test:e2e`
Expected: 地図の 4 件（canvas・タイル・出典・CSP）が FAIL、非対応の画面の 1 件は PASS

- [ ] **Step 4: 地図を実装する**

`src/map/gsiStyle.ts`:

```ts
import type { StyleSpecification } from 'maplibre-gl'

/** 地理院タイルの淡色地図（ズーム 2〜18） */
export const GSI_PALE_TILE_URL = 'https://cyberjapandata.gsi.go.jp/xyz/pale/{z}/{x}/{y}.png'

export interface Attribution {
  text: string
  url: string
}

export function createGsiPaleStyle(attribution: Attribution): StyleSpecification {
  return {
    version: 8,
    sources: {
      'gsi-pale': {
        type: 'raster',
        tiles: [GSI_PALE_TILE_URL],
        tileSize: 256,
        minzoom: 2,
        maxzoom: 18,
        attribution: `<a href="${attribution.url}" target="_blank" rel="noopener noreferrer">${attribution.text}</a>`,
      },
    },
    layers: [{ id: 'gsi-pale', type: 'raster', source: 'gsi-pale' }],
  }
}
```

`src/map/MapController.ts`:

```ts
import { Map as MapLibreMap, setWorkerUrl } from 'maplibre-gl'
import 'maplibre-gl/dist/maplibre-gl.css'
import workerUrl from 'maplibre-gl/dist/maplibre-gl-worker.mjs?worker&url'
import { type Attribution, createGsiPaleStyle } from './gsiStyle'

// MapLibre 6 は内部の Worker の URL を import.meta.url から組み立てるが、バンドル後はその場所に
// ファイルが無い。Vite に Worker をバンドルさせ、その URL を渡す
setWorkerUrl(workerUrl)

/** 初期表示: 日本全体（R01-5） */
export const INITIAL_VIEW = { center: [138.0, 36.0] as [number, number], zoom: 5 }

/** MapLibre を React の外で生成・破棄する（tech-spec §5.3） */
export class MapController {
  readonly map: MapLibreMap

  constructor(container: HTMLElement, attribution: Attribution) {
    this.map = new MapLibreMap({
      container,
      style: createGsiPaleStyle(attribution),
      center: INITIAL_VIEW.center,
      zoom: INITIAL_VIEW.zoom,
      maxZoom: 18,
      // 出典は常に表示する（tech-spec §16.1）。compact だと狭い画面で折りたたまれる
      attributionControl: { compact: false },
      keyboard: true,
    })
  }

  destroy(): void {
    this.map.remove()
  }
}
```

`src/ui/components/MapView.tsx`:

```tsx
import Box from '@mui/material/Box'
import { createContext, type ReactNode, useContext, useEffect, useRef, useState } from 'react'
import { MapController } from '../../map/MapController'
import { strings } from '../strings'

const MapControllerContext = createContext<MapController | null>(null)

/** 子孫から地図を操作するためのフック。地図の生成前は null */
export function useMapController(): MapController | null {
  return useContext(MapControllerContext)
}

interface Props {
  children?: ReactNode
}

/** 全画面の地図。生成は依存配列を空にした useEffect の中だけで行う（tech-spec §5.3） */
export function MapView({ children }: Props) {
  const containerRef = useRef<HTMLDivElement>(null)
  const [controller, setController] = useState<MapController | null>(null)

  useEffect(() => {
    const container = containerRef.current
    if (container === null) return
    const created = new MapController(container, strings.attribution)
    created.map.once('load', () => {
      container.dataset.mapLoaded = 'true'
    })
    setController(created)
    return () => {
      container.removeAttribute('data-map-loaded')
      created.destroy()
      setController(null)
    }
  }, [])

  return (
    <MapControllerContext value={controller}>
      <Box
        ref={containerRef}
        component="main"
        aria-label={strings.map.ariaLabel}
        sx={{ position: 'fixed', inset: 0 }}
      />
      {children}
    </MapControllerContext>
  )
}
```

`src/ui/App.tsx` の `<Box component="main" … />` を `<MapView />` に置き換え、`Box` の import を消して `import { MapView } from './components/MapView'` を足す。

- [ ] **Step 5: CSP を置く**

`public/_headers`（spec §4.8）:

```
/*
  Content-Security-Policy: default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline'; img-src 'self' data: blob: https://cyberjapandata.gsi.go.jp; connect-src 'self' https://cyberjapandata.gsi.go.jp; worker-src 'self' blob:; object-src 'none'; form-action 'self'; frame-ancestors 'none'; base-uri 'self'
  X-Content-Type-Options: nosniff
  Referrer-Policy: strict-origin-when-cross-origin
```

- [ ] **Step 6: E2E を通す**

Run: `pnpm test:e2e`
Expected: 5 件すべて PASS。CSP 違反が出た場合は、違反したディレクティブと URL を確かめ、必要なら `_headers` に足して理由を `.handoff/01-foundation.md` に書く

- [ ] **Step 7: WebKit の対応状況を一度だけ調べる（02 への申し送り）**

02 は `@webkit` タグのテストで DEM の復号を WebKit でも回す（R01-4）。WebKit のヘッドレスで WebGL 2 と Worker 内の OffscreenCanvas が使えるかを先に確かめておく。

`/tmp/claude-1000/-home-terapyon-dev-terapyon-raintrace/<セッション>/scratchpad/webkit-probe.mjs`（リポジトリには置かない）:

```js
import { webkit } from '@playwright/test'

const browser = await webkit.launch()
const page = await browser.newPage()
await page.goto('http://localhost:4175/')
const result = await page.evaluate(async () => {
  const webgl2 = document.createElement('canvas').getContext('webgl2') !== null
  const source = `self.onmessage = () => { const c = new OffscreenCanvas(4, 4); const g = c.getContext('2d', { willReadFrequently: true }); self.postMessage(g !== null) }`
  const worker = new Worker(URL.createObjectURL(new Blob([source], { type: 'text/javascript' })))
  const offscreen2d = await new Promise((resolve) => {
    worker.onmessage = (e) => resolve(e.data)
    worker.postMessage(null)
  })
  return { webgl2, offscreen2d }
})
console.log(JSON.stringify(result))
await browser.close()
```

Run: `pnpm build && (timeout 60 pnpm preview --port 4175 --strictPort > /dev/null 2>&1 &) ; curl -s -o /dev/null --retry 20 --retry-connrefused --retry-delay 1 http://localhost:4175/ && NODE_PATH=$PWD/node_modules node <scratchpad>/webkit-probe.mjs`

（`import` は `NODE_PATH` を見ないので、スクリプトは scratchpad ではなくリポジトリの直下に一時的に置いて実行し、実行後に消してもよい）

Expected: `{"webgl2":…,"offscreen2d":…}`。結果を `.handoff/01-foundation.md` の「02 への申し送り」に書く。どちらかが `false` なら、02 の `@webkit` テストの前提が崩れるので、レビュー役に報告する

- [ ] **Step 8: 型検査、ユニット、lint を通す**

Run: `pnpm typecheck && pnpm test && pnpm lint`
Expected: すべて成功

- [ ] **Step 9: Commit**

```bash
git add -A
git status --short
git commit -m "地図: 地理院の淡色地図、出典の常時表示、CSP"
```

---

### Task 5: Worker の骨組み（ping/pong、R01-6）

**Files:**
- Create: `tsconfig.worker.json`、`src/workers/simulation.worker.ts`
- Create: `src/bridge/SimulationClient.ts`、`src/bridge/SimulationClient.test.ts`
- Modify: `tsconfig.json`、`src/main.tsx`、`tests/e2e/smoke.spec.ts`（E2E の 4）

**Interfaces:**
- Consumes: `MainToWorkerMessage`、`WorkerToMainMessage`（Task 2）
- Produces（02 が拡張する）:
  - `interface WorkerPort { postMessage(message: MainToWorkerMessage): void; addEventListener(type: 'message', listener: (event: MessageEvent<WorkerToMainMessage>) => void): void; removeEventListener(同上): void; terminate(): void }`
  - `createSimulationWorker(): WorkerPort`
  - `class SimulationClient { constructor(createWorker?: () => WorkerPort); ping(timeoutMs?: number): Promise<void>; dispose(): void }`、`PING_TIMEOUT_MS = 5000`
  - ルート要素（`<html>`）の `data-worker-ready`（`"true"` または `"false"`）

- [ ] **Step 1: SimulationClient の失敗するテストを書く**

`src/bridge/SimulationClient.test.ts`（Worker は偽物に差し替える。Node 24 には `MessageEvent` がある）:

```ts
import { afterEach, describe, expect, it, vi } from 'vitest'
import type { MainToWorkerMessage, WorkerToMainMessage } from '../shared/protocol'
import { SimulationClient, type WorkerPort } from './SimulationClient'

type Listener = (event: MessageEvent<WorkerToMainMessage>) => void

class FakeWorker implements WorkerPort {
  readonly posted: MainToWorkerMessage[] = []
  readonly listeners = new Set<Listener>()
  terminated = false

  postMessage(message: MainToWorkerMessage): void {
    this.posted.push(message)
  }
  addEventListener(_type: 'message', listener: Listener): void {
    this.listeners.add(listener)
  }
  removeEventListener(_type: 'message', listener: Listener): void {
    this.listeners.delete(listener)
  }
  terminate(): void {
    this.terminated = true
  }
  reply(message: WorkerToMainMessage): void {
    for (const listener of this.listeners) listener(new MessageEvent('message', { data: message }))
  }
}

function setup() {
  const worker = new FakeWorker()
  const client = new SimulationClient(() => worker)
  return { worker, client }
}

afterEach(() => {
  vi.useRealTimers()
})

describe('SimulationClient', () => {
  it('ping を送り、同じ id の pong が返ると解決する', async () => {
    const { worker, client } = setup()
    const done = client.ping()
    expect(worker.posted).toEqual([{ type: 'ping', id: 1 }])
    worker.reply({ type: 'pong', id: 1 })
    await expect(done).resolves.toBeUndefined()
  })

  it('違う id の pong では解決せず、期限を過ぎると失敗する', async () => {
    vi.useFakeTimers()
    const { worker, client } = setup()
    const done = client.ping(5000)
    worker.reply({ type: 'pong', id: 999 })
    vi.advanceTimersByTime(5000)
    await expect(done).rejects.toThrow('5000ms')
  })

  it('ping のたびに id が増える', () => {
    const { worker, client } = setup()
    void client.ping().catch(() => {})
    void client.ping().catch(() => {})
    expect(worker.posted.map((m) => m.id)).toEqual([1, 2])
    client.dispose()
  })

  it('dispose で Worker を終了し、受信をやめる', () => {
    const { worker, client } = setup()
    client.dispose()
    expect(worker.terminated).toBe(true)
    expect(worker.listeners.size).toBe(0)
  })
})
```

Run: `pnpm test`
Expected: FAIL（`./SimulationClient` が見つからない）

- [ ] **Step 2: SimulationClient を実装する**

`src/bridge/SimulationClient.ts`:

```ts
import type { MainToWorkerMessage, WorkerToMainMessage } from '../shared/protocol'

/** SimulationClient が使う Worker の機能。テストでは偽物に差し替える */
export interface WorkerPort {
  postMessage(message: MainToWorkerMessage): void
  addEventListener(
    type: 'message',
    listener: (event: MessageEvent<WorkerToMainMessage>) => void,
  ): void
  removeEventListener(
    type: 'message',
    listener: (event: MessageEvent<WorkerToMainMessage>) => void,
  ): void
  terminate(): void
}

/** Vite が Worker をバンドルできるよう、new Worker(new URL(...)) をこの形のまま書く */
export function createSimulationWorker(): WorkerPort {
  return new Worker(new URL('../workers/simulation.worker.ts', import.meta.url), {
    type: 'module',
    name: 'simulation',
  })
}

export const PING_TIMEOUT_MS = 5000

/** メインスレッドで Worker を所有する（tech-spec §5.1）。01 では疎通の確認だけを行う */
export class SimulationClient {
  private readonly worker: WorkerPort
  private readonly pending = new Map<number, () => void>()
  private nextId = 1

  private readonly onMessage = (event: MessageEvent<WorkerToMainMessage>): void => {
    const message = event.data
    switch (message.type) {
      case 'pong': {
        const resolve = this.pending.get(message.id)
        if (resolve !== undefined) {
          this.pending.delete(message.id)
          resolve()
        }
        break
      }
    }
  }

  constructor(createWorker: () => WorkerPort = createSimulationWorker) {
    this.worker = createWorker()
    this.worker.addEventListener('message', this.onMessage)
  }

  /** ping を送り、pong が返れば解決する。timeoutMs 以内に返らなければ失敗する */
  ping(timeoutMs = PING_TIMEOUT_MS): Promise<void> {
    const id = this.nextId++
    return new Promise((resolve, reject) => {
      const timer = setTimeout(() => {
        this.pending.delete(id)
        reject(new Error(`Worker が ${timeoutMs}ms 以内に応答しませんでした`))
      }, timeoutMs)
      this.pending.set(id, () => {
        clearTimeout(timer)
        resolve()
      })
      this.worker.postMessage({ type: 'ping', id })
    })
  }

  dispose(): void {
    this.worker.removeEventListener('message', this.onMessage)
    this.worker.terminate()
    this.pending.clear()
  }
}
```

Run: `pnpm test`
Expected: PASS

- [ ] **Step 3: Worker を実装する**

`tsconfig.worker.json`（DOM と WebWorker の lib を同じコンパイル単位に置かない。tech-spec §10.3）:

```json
{
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    "noEmit": true,
    "tsBuildInfoFile": "node_modules/.tmp/tsc/worker.tsbuildinfo",
    "lib": ["ES2023", "WebWorker"],
    "noUncheckedIndexedAccess": true
  },
  "include": ["src/workers"],
  "exclude": ["src/**/*.test.ts"],
  "references": [{ "path": "./tsconfig.sim.json" }, { "path": "./tsconfig.core.json" }]
}
```

`tsconfig.json` の `references` に `{ "path": "./tsconfig.worker.json" }` を足す。

`src/workers/simulation.worker.ts`:

```ts
import type { MainToWorkerMessage, WorkerToMainMessage } from '../shared/protocol'

declare const self: DedicatedWorkerGlobalScope

function post(message: WorkerToMainMessage): void {
  self.postMessage(message)
}

self.addEventListener('message', (event: MessageEvent<MainToWorkerMessage>) => {
  const message = event.data
  switch (message.type) {
    case 'ping':
      post({ type: 'pong', id: message.id })
      break
  }
})
```

- [ ] **Step 4: E2E の失敗するテストを書く**

`tests/e2e/smoke.spec.ts` の「出典が表示される」の後に足す:

```ts
test('Worker が起動し、ルート要素に data-worker-ready="true" が付く', async ({ page }) => {
  await page.goto('/')
  await expect(page.locator('html')).toHaveAttribute('data-worker-ready', 'true')
})
```

Run: `pnpm test:e2e`
Expected: この 1 件が FAIL（属性が付かない）

- [ ] **Step 5: 起動時に疎通を確かめる**

`src/main.tsx` に import と関数を足し、描画の後で呼ぶ:

```tsx
import { SimulationClient } from './bridge/SimulationClient'
```

```tsx
/** 起動時に Worker と ping を往復させ、結果をルート要素に記す（spec 01 §4.4、§6） */
function checkWorker(): void {
  const root = document.documentElement
  new SimulationClient().ping().then(
    () => {
      root.dataset.workerReady = 'true'
    },
    (error: unknown) => {
      console.error(error)
      root.dataset.workerReady = 'false'
    },
  )
}
```

`createRoot(...)` の呼び出しを次に置き換える:

```tsx
const missingFeatures = detectMissingFeatures()

createRoot(container).render(
  <StrictMode>
    <App missingFeatures={missingFeatures} />
  </StrictMode>,
)

if (missingFeatures.length === 0) checkWorker()
```

Run: `pnpm test:e2e`
Expected: 6 件すべて PASS

- [ ] **Step 6: Worker がバンドルされていることを確かめる**

Run: `ls dist/assets | grep -E 'simulation|maplibre-gl-worker'`
Expected: `simulation-*.js`（または `simulation.worker-*.js`）と `maplibre-gl-worker-*.js` がある

- [ ] **Step 7: 型検査、ユニット、lint を通す**

Run: `pnpm typecheck && pnpm test && pnpm lint`
Expected: すべて成功

- [ ] **Step 8: Commit**

```bash
git add -A
git status --short
git commit -m "Worker の骨組み: ping と pong の往復、SimulationClient"
```

---

### Task 6: 依存規則（dependency-cruiser）とローカルのフック（lefthook）

**Files:**
- Create: `.dependency-cruiser.mjs`、`lefthook.yml`
- Modify: `package.json`、`pnpm-workspace.yaml`

**Interfaces:**
- Produces: `pnpm depcheck`、`prepare`（`lefthook install`）。以降のコミットで pre-commit（Biome）が動く

- [ ] **Step 1: 依存を入れる**

Run: `pnpm add -DE dependency-cruiser@18.2.0 lefthook@2.1.12`
Expected: lefthook の postinstall で `ERR_PNPM_IGNORED_BUILDS` になる場合は、`pnpm-workspace.yaml` の `allowBuilds` に `lefthook: false` を足して `pnpm install` をやり直す（tech-spec §13.3: lefthook は allowBuilds で許可せず、`prepare` でフックを入れる）

- [ ] **Step 2: 依存規則を書く**

`.dependency-cruiser.mjs`（tech-spec §4.2 の 7 規則）:

```js
/** レイヤー境界の規則（tech-spec §4.2）。`pnpm depcheck` で検査する */
/** @type {import('dependency-cruiser').IConfiguration} */
export default {
  forbidden: [
    {
      name: 'core-is-pure',
      comment:
        'src/simulation と src/dem は、自ディレクトリの外（src 内の他のディレクトリ、外部パッケージ、Node の組み込み）に依存しない',
      severity: 'error',
      from: { path: '^src/(simulation|dem)/', pathNot: '\\.test\\.ts$' },
      to: { pathNot: '^src/$1/' },
    },
    {
      name: 'types-only-from-core',
      comment: 'state・renderer・shared から simulation・dem へは型だけを import する',
      severity: 'error',
      from: { path: '^src/(state|renderer|shared)/' },
      to: { path: '^src/(simulation|dem)/', dependencyTypesNot: ['type-only'] },
    },
    {
      name: 'no-react-outside-ui',
      comment: 'renderer と bridge は React・MUI・Emotion に依存しない',
      severity: 'error',
      from: { path: '^src/(renderer|bridge)/' },
      to: { path: 'node_modules/(react|react-dom|@mui/[^/]+|@emotion/[^/]+)/' },
    },
    {
      name: 'workers-not-imported',
      comment: 'Worker は new Worker(new URL(...), { type: "module" }) でのみ起動し、静的に import しない',
      severity: 'error',
      from: { pathNot: '^src/workers/' },
      to: { path: '^src/workers/' },
    },
    {
      name: 'workers-isolated',
      comment: 'Worker からは simulation・dem・shared 以外の自作モジュールを import しない',
      severity: 'error',
      from: { path: '^src/workers/' },
      to: { path: '^src/', pathNot: '^src/(workers|simulation|dem|shared)/' },
    },
    {
      name: 'no-circular',
      severity: 'error',
      from: {},
      to: { circular: true },
    },
    {
      name: 'no-orphans',
      comment:
        'どこからも参照されないモジュールを禁止する。エントリポイント、Worker のエントリ（new URL で起動するため依存として辿れない）、テスト、型宣言は除く',
      severity: 'error',
      from: {
        orphan: true,
        pathNot: [
          '\\.(test|spec)\\.tsx?$',
          '\\.d\\.ts$',
          '^src/main\\.tsx$',
          '^src/workers/[^/]+\\.worker\\.ts$',
        ],
      },
      to: {},
    },
  ],
  options: {
    doNotFollow: { path: 'node_modules' },
    // import type を区別する（types-only-from-core に必要。tech-spec §4.2）
    tsPreCompilationDeps: 'specify',
    enhancedResolveOptions: {
      exportsFields: ['exports'],
      conditionNames: ['import', 'require', 'node', 'default', 'types'],
      extensions: ['.ts', '.tsx', '.js', '.mjs', '.json'],
    },
  },
}
```

`package.json` の scripts に足す:

```json
"depcheck": "depcruise src --config .dependency-cruiser.mjs"
```

Run: `pnpm depcheck`
Expected: `no dependency violations found`。`src/simulation/constants.ts` は `constants.test.ts` から、`src/dem/tileMath.ts` は `tileMath.test.ts` から参照されるので orphan にならない

- [ ] **Step 3: 規則が違反を捕まえることを確かめる**

一時ファイルを作って違反させ、検出されることを確かめてから消す:

```bash
printf "import { useState } from 'react'\nexport const s = useState\n" > src/bridge/tmpReact.ts
printf "import { MapController } from '../map/MapController'\nexport const m = MapController\n" > src/workers/tmpIsolated.ts
printf "import { real } from '../dem/tileMath.ts'\nexport const r = real\n" > src/shared/tmpValue.ts
pnpm depcheck; echo "exit=$?"
rm src/bridge/tmpReact.ts src/workers/tmpIsolated.ts src/shared/tmpValue.ts
```

`src/shared/tmpValue.ts` の `real` は存在しない名前だが、依存規則の検査には影響しない（型検査はしない）。値の import なので `types-only-from-core` に当たる。

Expected: `no-react-outside-ui`、`workers-isolated`、`types-only-from-core` がそれぞれ error として出て、`exit=1`

- [ ] **Step 4: spec §4.5 の一度だけの検証を行い、結果を控える**

結果（コマンドの出力の要点）を `.handoff/01-foundation.md` の記録欄に書く（Task 9 で PR 本文にまとめる）。各検証の後、一時ファイルを消して `pnpm typecheck` が通る状態に戻す。

```bash
# 1. src/simulation の arr[0] + 1 は通り、src/ui の同じ式は失敗する
printf "export const f = (arr: number[]): number => arr[0] + 1\n" > src/simulation/tmpIndex.ts
pnpm typecheck; echo "sim exit=$?"
printf "export const f = (arr: number[]): number => arr[0] + 1\n" > src/ui/tmpIndex.ts
pnpm typecheck; echo "ui exit=$?"
rm src/simulation/tmpIndex.ts src/ui/tmpIndex.ts

# 2. src/simulation から react を import すると、dependency-cruiser と tsc -b が失敗する
printf "import { useState } from 'react'\nexport const s = useState\n" > src/simulation/tmpReact.ts
pnpm depcheck; echo "depcruise exit=$?"
pnpm typecheck; echo "tsc exit=$?"
rm src/simulation/tmpReact.ts

# 3. src/dem で document を参照すると tsc -b が失敗する
printf "export const t = document.title\n" > src/dem/tmpDom.ts
pnpm typecheck; echo "dem exit=$?"
rm src/dem/tmpDom.ts

# 4. .ts の拡張子を付けた相対 import が、型宣言の出力と参照する側の解決の両方で通る
printf "import { FLOW_THRESHOLD_M } from './constants.ts'\nexport const twice = FLOW_THRESHOLD_M * 2\n" > src/simulation/tmpExt.ts
printf "import { twice } from '../simulation/tmpExt'\nexport const t = twice\n" > src/ui/tmpExt.ts
pnpm typecheck; echo "ext exit=$?"
cat node_modules/.tmp/tsc/sim/simulation/tmpExt.d.ts
rm src/simulation/tmpExt.ts src/ui/tmpExt.ts
pnpm typecheck
```

Expected:
- 1: `sim exit=0`、`ui exit=` が 0 以外（TS2532）
- 2: `depcruise exit=` が 0 以外（`core-is-pure`）。**tsc は失敗しない可能性がある**（`@types/react` は DOM の型が無くても読めるため）。その場合は、その事実をそのまま記録する。tech-spec §4.2 の二重の強制のうち、外部パッケージの禁止は dependency-cruiser が担う
- 3: `dem exit=` が 0 以外（TS2584）
- 4: `ext exit=0`。`tmpExt.d.ts` が出力される（`tileMath.test.ts` が `./tileMath.ts` を import して型検査が通っていることも、参照する側の解決の証拠になる）

- [ ] **Step 5: lefthook を設定する**

`lefthook.yml`（tech-spec §12.2）:

```yaml
# ローカルの品質ゲート（tech-spec §12.2）。重い検査は pre-push に置く
pre-commit:
  commands:
    biome:
      glob: "*.{js,mjs,ts,tsx,json,jsonc}"
      run: pnpm exec biome check --write --no-errors-on-unmatched --files-ignore-unknown=true {staged_files}
      stage_fixed: true
pre-push:
  commands:
    typecheck:
      run: pnpm exec tsc -b
    unit:
      run: pnpm exec vitest run
```

`package.json` の scripts に足す:

```json
"prepare": "lefthook install"
```

Run: `pnpm install && ls .git/hooks | grep -E '^pre-(commit|push)$'`
Expected: `pre-commit` と `pre-push` がある

- [ ] **Step 6: lint を通す**

Run: `pnpm lint && pnpm depcheck && pnpm typecheck`
Expected: すべて成功

- [ ] **Step 7: Commit（pre-commit のフックが動くことも確かめる）**

```bash
git add -A
git status --short
git commit -m "品質ゲート: dependency-cruiser のレイヤー規則と lefthook"
```

Expected: コミットの出力に lefthook の `biome` が表示される

---

### Task 7: バンドル予算とライセンス検査

**Files:**
- Create: `scripts/lib/bundleBudget.mjs`、`scripts/lib/bundleBudget.test.mjs`、`scripts/check-bundle-size.mjs`
- Create: `scripts/lib/licensePolicy.mjs`、`scripts/lib/licensePolicy.test.mjs`、`scripts/check-licenses.mjs`
- Modify: `vite.config.ts`、`vitest.config.ts`、`package.json`

**Interfaces:**
- Produces:
  - `bundleBudget.mjs`: `BUDGET = { initialKb: 400, totalKb: 1200 }`、`initialFiles(manifest): Set<string>`、`evaluateBudget(files, initial, budget?): { rows, initialBytes, totalBytes, violations }`、`duplicatedModules(chunkModules): { id, chunks }[]`、`kb(bytes): string`
  - `licensePolicy.mjs`: `classifyLicense(expression: string): 'ok' | 'forbidden' | 'unknown'`、`evaluateLicenses(report): { forbidden: string[], unknown: string[] }`
  - ビルドの出力に `dist/.vite/chunk-modules.json`（メインスレッドのチャンク → モジュールの一覧）
  - scripts: `pnpm size`、`pnpm licenses`

KB は 1000 バイトとする（Vite のビルドの表示と数字を揃えるため）。

- [ ] **Step 1: 予算の判定の失敗するテストを書く**

`vitest.config.ts` の `include` を `['src/**/*.test.ts', 'scripts/**/*.test.mjs']` にする。

`scripts/lib/bundleBudget.test.mjs`:

```js
import { describe, expect, it } from 'vitest'
import { duplicatedModules, evaluateBudget, initialFiles } from './bundleBudget.mjs'

const manifest = {
  'index.html': { file: 'assets/index-a.js', isEntry: true, imports: ['_ui-b.js', '_map-c.js'] },
  '_ui-b.js': { file: 'assets/ui-b.js' },
  '_map-c.js': { file: 'assets/map-c.js', imports: ['_ui-b.js'] },
  'src/lazy.ts': { file: 'assets/lazy-d.js', isDynamicEntry: true, imports: ['_ui-b.js'] },
}

describe('initialFiles', () => {
  it('エントリと、そこから静的に import されるチャンクを集める', () => {
    expect([...initialFiles(manifest)].sort()).toEqual([
      'assets/index-a.js',
      'assets/map-c.js',
      'assets/ui-b.js',
    ])
  })
})

describe('evaluateBudget', () => {
  const files = [
    { file: 'assets/index-a.js', gzipBytes: 50_000 },
    { file: 'assets/ui-b.js', gzipBytes: 150_000 },
    { file: 'assets/map-c.js', gzipBytes: 170_000 },
    { file: 'assets/lazy-d.js', gzipBytes: 300_000 },
  ]
  const initial = initialFiles(manifest)

  it('予算内なら違反なし。大きい順に並べる', () => {
    const result = evaluateBudget(files, initial)
    expect(result.initialBytes).toBe(370_000)
    expect(result.totalBytes).toBe(670_000)
    expect(result.violations).toEqual([])
    expect(result.rows.map((r) => r.file)[0]).toBe('assets/lazy-d.js')
    expect(result.rows.find((r) => r.file === 'assets/lazy-d.js')?.initial).toBe(false)
  })

  it('初期ロードと総量のそれぞれの超過を報告する', () => {
    const result = evaluateBudget(files, initial, { initialKb: 300, totalKb: 600 })
    expect(result.violations).toHaveLength(2)
    expect(result.violations[0]).toContain('初期ロード')
    expect(result.violations[1]).toContain('総量')
  })
})

describe('duplicatedModules', () => {
  it('複数のチャンクに含まれるモジュールだけを返す', () => {
    expect(
      duplicatedModules({
        'assets/a.js': ['src/x.ts', 'src/y.ts'],
        'assets/b.js': ['src/y.ts'],
      }),
    ).toEqual([{ id: 'src/y.ts', chunks: ['assets/a.js', 'assets/b.js'] }])
  })
})
```

Run: `pnpm test`
Expected: FAIL（`./bundleBudget.mjs` が見つからない）

- [ ] **Step 2: 予算の判定を実装する**

`scripts/lib/bundleBudget.mjs`:

```js
/** バンドル予算の判定（tech-spec §14.2）。I/O を持たない */

export const BUDGET = { initialKb: 400, totalKb: 1200 }

/** バイト数を KB（1000 バイト）の文字列にする */
export const kb = (bytes) => (bytes / 1000).toFixed(1)

/**
 * 初期ロードのチャンク（エントリと、そこから静的に import されるチャンク）を求める
 * @param {Record<string, { file: string, isEntry?: boolean, imports?: string[] }>} manifest Vite のマニフェスト
 * @returns {Set<string>} dist からの相対パス
 */
export function initialFiles(manifest) {
  const result = new Set()
  const visit = (key) => {
    const chunk = manifest[key]
    if (chunk === undefined || result.has(chunk.file)) return
    result.add(chunk.file)
    for (const imported of chunk.imports ?? []) visit(imported)
  }
  for (const [key, chunk] of Object.entries(manifest)) {
    if (chunk.isEntry) visit(key)
  }
  return result
}

/**
 * @param {{ file: string, gzipBytes: number }[]} files dist/assets の JS
 * @param {Set<string>} initial 初期ロードのファイル
 * @param {{ initialKb: number, totalKb: number }} budget
 */
export function evaluateBudget(files, initial, budget = BUDGET) {
  const rows = files
    .map((f) => ({ ...f, initial: initial.has(f.file) }))
    .sort((a, b) => b.gzipBytes - a.gzipBytes)
  const initialBytes = rows.filter((r) => r.initial).reduce((sum, r) => sum + r.gzipBytes, 0)
  const totalBytes = rows.reduce((sum, r) => sum + r.gzipBytes, 0)
  const violations = []
  if (initialBytes > budget.initialKb * 1000) {
    violations.push(`初期ロード ${kb(initialBytes)} KB が上限 ${budget.initialKb} KB を超えています`)
  }
  if (totalBytes > budget.totalKb * 1000) {
    violations.push(`総量 ${kb(totalBytes)} KB が上限 ${budget.totalKb} KB を超えています`)
  }
  return { rows, initialBytes, totalBytes, violations }
}

/**
 * 複数のチャンクに含まれるモジュールを返す
 * @param {Record<string, string[]>} chunkModules チャンクのファイル名 → モジュール ID の一覧
 */
export function duplicatedModules(chunkModules) {
  const owners = new Map()
  for (const [chunk, ids] of Object.entries(chunkModules)) {
    for (const id of ids) owners.set(id, [...(owners.get(id) ?? []), chunk])
  }
  return [...owners]
    .filter(([, chunks]) => chunks.length > 1)
    .map(([id, chunks]) => ({ id, chunks }))
}
```

Run: `pnpm test`
Expected: PASS

- [ ] **Step 3: チャンク分けと、チャンクのモジュール一覧の出力を設定する**

`vite.config.ts` を次に置き換える:

```ts
import { relative } from 'node:path'
import { cloudflare } from '@cloudflare/vite-plugin'
import react from '@vitejs/plugin-react'
import { defineConfig, type Plugin } from 'vite'

/**
 * メインスレッドの各チャンクに入ったモジュールを dist/.vite/chunk-modules.json に書き出す。
 * scripts/check-bundle-size.mjs が、チャンクの間の重複を報告するのに使う（spec 01 §4.9）
 */
function chunkModulesReport(): Plugin {
  return {
    name: 'raintrace:chunk-modules',
    apply: 'build',
    generateBundle(_options, bundle) {
      const report: Record<string, string[]> = {}
      for (const output of Object.values(bundle)) {
        if (output.type !== 'chunk') continue
        report[output.fileName] = output.moduleIds
          .filter((id) => !id.startsWith('\0'))
          .map((id) => relative(process.cwd(), id))
      }
      this.emitFile({
        type: 'asset',
        fileName: '.vite/chunk-modules.json',
        source: JSON.stringify(report, null, 2),
      })
    },
  }
}

export default defineConfig({
  plugins: [react(), cloudflare(), chunkModulesReport()],
  // Worker は { type: 'module' } で起動するので ES モジュールとして出力する
  worker: { format: 'es' },
  build: {
    manifest: true,
    rolldownOptions: {
      output: {
        // 地図系・UI 系・アプリ本体に分ける（spec 01 §4.9）。
        // UI 系は react・react-dom・@mui・@emotion と、それらが引き込む依存（scheduler、stylis など）
        codeSplitting: {
          groups: [
            { name: 'map', test: /node_modules[\\/]maplibre-gl[\\/]/, priority: 20 },
            { name: 'ui', test: /node_modules[\\/]/, priority: 10 },
          ],
        },
      },
    },
  },
})
```

Run: `pnpm build && ls dist/assets && head -c 300 dist/.vite/chunk-modules.json`
Expected: `map-*.js`、`ui-*.js`、`index-*.js`（アプリ本体）、`maplibre-gl-worker-*.js`、`simulation*.js` がある。`chunk-modules.json` にチャンクごとのモジュールが並ぶ。`tsconfig.node.json` の型検査で `output.moduleIds` や `codeSplitting` が型エラーになる場合は、Vite 8 の型（`node_modules/vite/dist/node/index.d.ts` の `rolldownOptions`）に合わせて直す

- [ ] **Step 4: 予算の検査スクリプトを書く**

`scripts/check-bundle-size.mjs`（依存なし。`node:fs`・`node:path`・`node:zlib` のみ）:

```js
/** dist の JS を gzip で集計し、バンドル予算（tech-spec §14.2）を超えたら失敗する */
import { existsSync, readdirSync, readFileSync } from 'node:fs'
import { join } from 'node:path'
import { gzipSync } from 'node:zlib'
import { duplicatedModules, evaluateBudget, initialFiles, kb } from './lib/bundleBudget.mjs'

const dist = 'dist'
const manifest = JSON.parse(readFileSync(join(dist, '.vite/manifest.json'), 'utf8'))
const files = readdirSync(join(dist, 'assets'))
  .filter((name) => name.endsWith('.js'))
  .map((name) => {
    const file = `assets/${name}`
    return { file, gzipBytes: gzipSync(readFileSync(join(dist, file)), { level: 9 }).length }
  })

const result = evaluateBudget(files, initialFiles(manifest))

console.log('| チャンク | gzip (KB) | 初期ロード |')
console.log('|---|---:|:---:|')
for (const row of result.rows) {
  console.log(`| ${row.file} | ${kb(row.gzipBytes)} | ${row.initial ? '○' : ''} |`)
}
console.log(`\n初期ロード: ${kb(result.initialBytes)} KB / 総量: ${kb(result.totalBytes)} KB`)
console.log('（Worker のチャンクは、起動時に読まれても初期ロードには数えない。spec 01 §4.9 の定義による）')

const reportPath = join(dist, '.vite/chunk-modules.json')
if (existsSync(reportPath)) {
  const duplicates = duplicatedModules(JSON.parse(readFileSync(reportPath, 'utf8')))
  if (duplicates.length === 0) {
    console.log('チャンクの間で重複したモジュール: なし')
  } else {
    console.log(`チャンクの間で重複したモジュール: ${duplicates.length} 件`)
    for (const { id, chunks } of duplicates) console.log(`  ${id}: ${chunks.join(', ')}`)
  }
}

if (result.violations.length > 0) {
  for (const message of result.violations) console.error(message)
  process.exit(1)
}
```

`package.json` の scripts に足す:

```json
"size": "node scripts/check-bundle-size.mjs"
```

Run: `pnpm size`
Expected: チャンクごとの表と合計が出て、初期ロードが 400KB 以下、総量が 1.2MB 以下で終了コード 0。表の数字を `.handoff/01-foundation.md` の記録欄に控える（Task 9 で tech-spec §14.2 に書く）

失敗させて確かめる: `node -e "import('./scripts/lib/bundleBudget.mjs').then(m => console.log(m.evaluateBudget([{ file: 'a', gzipBytes: 500000 }], new Set(['a'])).violations))"`
Expected: 初期ロードの超過のメッセージが 1 件出る

- [ ] **Step 5: ライセンスの判定の失敗するテストを書く**

`scripts/lib/licensePolicy.test.mjs`:

```js
import { describe, expect, it } from 'vitest'
import { classifyLicense, evaluateLicenses } from './licensePolicy.mjs'

describe('classifyLicense', () => {
  it.each(['MIT', 'ISC', 'Apache-2.0', 'BSD-3-Clause', '0BSD', 'MIT AND Apache-2.0'])(
    '%s は許容',
    (expression) => {
      expect(classifyLicense(expression)).toBe('ok')
    },
  )

  it.each(['GPL-3.0', 'GPL-2.0-or-later', 'LGPL-2.1', 'AGPL-3.0-only', 'GPL-2.0+', 'MIT AND GPL-3.0'])(
    '%s は禁止',
    (expression) => {
      expect(classifyLicense(expression)).toBe('forbidden')
    },
  )

  it('OR で選べるなら、許容の側を選べるので許容', () => {
    expect(classifyLicense('(MIT OR GPL-3.0)')).toBe('ok')
    expect(classifyLicense('GPL-2.0 OR LGPL-3.0')).toBe('forbidden')
  })

  it.each(['Unknown', '', 'UNLICENSED', 'SEE LICENSE IN LICENSE.md'])('%s は判定できない', (e) => {
    expect(classifyLicense(e)).toBe('unknown')
  })
})

describe('evaluateLicenses', () => {
  it('pnpm licenses list --json の形から、禁止と判定できないものを集める', () => {
    const report = {
      MIT: [{ name: 'a', versions: ['1.0.0'] }],
      'GPL-3.0': [{ name: 'b', versions: ['2.0.0', '2.1.0'] }],
      Unknown: [{ name: 'c', versions: ['0.1.0'] }],
    }
    expect(evaluateLicenses(report)).toEqual({
      forbidden: ['b@2.0.0, 2.1.0 (GPL-3.0)'],
      unknown: ['c@0.1.0 (Unknown)'],
    })
  })
})
```

Run: `pnpm test`
Expected: FAIL（`./licensePolicy.mjs` が見つからない）

- [ ] **Step 6: ライセンスの判定を実装する**

`scripts/lib/licensePolicy.mjs`:

```js
/**
 * 依存のライセンスの判定（tech-spec §16.2、R01-1 の MIT に合わせる）。I/O を持たない。
 * GPL・AGPL・LGPL の系統を禁止する。SPDX の式は OR と AND だけを扱い、括弧の入れ子は平らにして読む
 */

const isCopyleft = (id) => /^(A|L)?GPL(-|\+|$)/i.test(id)

/**
 * @param {string} expression SPDX の式（例: "MIT"、"(MIT OR GPL-3.0)"、"MIT AND Apache-2.0"）
 * @returns {'ok' | 'forbidden' | 'unknown'}
 */
export function classifyLicense(expression) {
  const trimmed = expression.trim()
  if (trimmed === '' || /^(unknown|unlicensed|see license in)/i.test(trimmed)) return 'unknown'
  // OR はどれか1つを選べる。AND はすべてに従う必要がある
  const alternatives = trimmed.replace(/[()]/g, ' ').split(/\s+OR\s+/i)
  const allowed = alternatives.some((alternative) =>
    alternative
      .split(/\s+AND\s+/i)
      .map((part) => part.trim())
      .filter((part) => part !== '')
      .every((part) => !isCopyleft(part)),
  )
  return allowed ? 'ok' : 'forbidden'
}

/**
 * @param {Record<string, { name: string, versions: string[] }[]>} report `pnpm licenses list --json` の出力
 */
export function evaluateLicenses(report) {
  const forbidden = []
  const unknown = []
  for (const [license, packages] of Object.entries(report)) {
    const verdict = classifyLicense(license)
    if (verdict === 'ok') continue
    for (const pkg of packages) {
      const label = `${pkg.name}@${pkg.versions.join(', ')} (${license})`
      ;(verdict === 'forbidden' ? forbidden : unknown).push(label)
    }
  }
  return { forbidden, unknown }
}
```

Run: `pnpm test`
Expected: PASS

- [ ] **Step 7: ライセンスの検査スクリプトを書く**

`scripts/check-licenses.mjs`:

```js
/** 本番の依存のライセンスを検査し、GPL 系が含まれていたら失敗する（tech-spec §16.2） */
import { execFileSync } from 'node:child_process'
import { evaluateLicenses } from './lib/licensePolicy.mjs'

const output = execFileSync('pnpm', ['licenses', 'list', '--json', '--prod'], { encoding: 'utf8' })
const report = JSON.parse(output)
const { forbidden, unknown } = evaluateLicenses(report)

console.log(`ライセンスの種類: ${Object.keys(report).sort().join(', ')}`)
for (const label of unknown) console.warn(`警告: ライセンスを判定できません: ${label}`)
if (forbidden.length > 0) {
  for (const label of forbidden) console.error(`禁止されたライセンス: ${label}`)
  process.exit(1)
}
console.log('禁止されたライセンスはありません')
```

`package.json` の scripts に足す:

```json
"licenses": "node scripts/check-licenses.mjs"
```

Run: `pnpm licenses`
Expected: 種類の一覧（MIT、ISC、BSD-3-Clause など）と「禁止されたライセンスはありません」。終了コード 0

- [ ] **Step 8: 全体を通す**

Run: `pnpm lint && pnpm typecheck && pnpm depcheck && pnpm test:coverage && pnpm test:e2e`
Expected: すべて成功（チャンク分けを変えても E2E が通る）

- [ ] **Step 9: Commit**

```bash
git add -A
git status --short
git commit -m "バンドル予算とライセンスの検査、チャンクを地図系・UI 系・アプリ本体に分ける"
```

---

### Task 8: CI/CD（GitHub Actions）

**Files:**
- Create: `.github/actions/setup/action.yml`、`.github/workflows/ci.yml`

**Interfaces:**
- Consumes: `pnpm lint`・`typecheck`・`depcheck`・`test:coverage`・`build`・`size`・`licenses`・`test:e2e`
- Produces: 必須チェックのジョブ名 `quality`・`test`・`build`・`e2e`（ユーザーがブランチ保護に設定する）。Secrets `CLOUDFLARE_API_TOKEN`・`CLOUDFLARE_ACCOUNT_ID`、environment `staging`・`production`

push できるのはユーザーだけなので、この Task では手元で確かめられる範囲（YAML の構文、コマンドの実在）を確かめ、実際の実行は push 後にユーザーと確認する。

- [ ] **Step 1: セットアップの composite action を書く**

`.github/actions/setup/action.yml`:

```yaml
name: setup
description: pnpm と Node を用意し、依存をインストールする（tech-spec §12.3）
runs:
  using: composite
  steps:
    # package.json の packageManager の版を使う
    - uses: pnpm/action-setup@0977fd99725f1db4007ccb2928dbb4e90d06cc86 # v6.0.10
    - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
      with:
        node-version-file: .nvmrc
        cache: pnpm
    - run: pnpm install --frozen-lockfile
      shell: bash
```

- [ ] **Step 2: ワークフローを書く**

`.github/workflows/ci.yml`:

```yaml
name: CI

on:
  pull_request:
  push:
    branches: [main]
    tags: ['v*']
  schedule:
    # 毎週月曜 09:00（日本時間）。audit で新しく公開された脆弱性を拾う
    - cron: '0 0 * * 1'

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  quality:
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: ./.github/actions/setup
      - run: pnpm lint
      - run: pnpm typecheck
      - run: pnpm depcheck

  test:
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: ./.github/actions/setup
      - run: pnpm test:coverage

  build:
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: ./.github/actions/setup
      - run: pnpm build
      - run: pnpm size | tee -a "$GITHUB_STEP_SUMMARY"
      - run: pnpm licenses

  e2e:
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: ./.github/actions/setup
      - run: pnpm exec playwright install --with-deps chromium webkit
      - run: pnpm test:e2e

  audit:
    # 必須にしない。PR では警告だけを出し、週次の実行で見つけたら Issue を起票する（tech-spec §12.4）
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    permissions:
      contents: read
      issues: write
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: ./.github/actions/setup
      - id: audit
        run: |
          set +e
          pnpm audit --audit-level=high > audit.txt 2>&1
          echo "status=$?" >> "$GITHUB_OUTPUT"
          cat audit.txt
      - if: steps.audit.outputs.status != '0' && github.event_name != 'schedule'
        run: echo "::warning title=pnpm audit::high 以上の脆弱性があります。ログを確認してください"
      - if: steps.audit.outputs.status != '0' && github.event_name == 'schedule'
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh issue create --title "pnpm audit で high 以上の脆弱性（$(date -u +%F)）" --body-file audit.txt

  deploy-preview:
    # 同じリポジトリのブランチからの PR のみ（フォークには Secrets が渡らない）。
    # プレビューはステージングの Worker にバージョンとして上げ、本番の Worker には触れない
    if: github.event_name == 'pull_request' && github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: ./.github/actions/setup
      - run: pnpm build
        env:
          CLOUDFLARE_ENV: staging
      - name: プレビュー用のバージョンを上げる
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          HEAD_REF: ${{ github.head_ref }}
        run: |
          alias=$(printf '%s' "$HEAD_REF" | tr '[:upper:]' '[:lower:]' | sed -E 's/[^a-z0-9-]+/-/g; s/^-+//; s/-+$//' | cut -c1-40)
          pnpm exec wrangler versions upload --preview-alias "$alias" --message "PR #${{ github.event.pull_request.number }} ${GITHUB_SHA::7}" | tee upload.log
          {
            echo "### プレビュー"
            grep -Eo 'https://[^ ]+\.workers\.dev' upload.log | sort -u | sed 's/^/- /'
          } >> "$GITHUB_STEP_SUMMARY"

  deploy-staging:
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    needs: [quality, test, build, e2e]
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    environment: staging
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: ./.github/actions/setup
      # 環境はビルド時に決まる。dist/wrangler.json が raintrace-staging 向けに出力される
      - run: pnpm build
        env:
          CLOUDFLARE_ENV: staging
      - run: pnpm exec wrangler deploy
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}

  deploy-production:
    # タグ付きリリース（R01-3）。タグの push でも必須ジョブが回り、その成功が前提
    if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/v')
    needs: [quality, test, build, e2e]
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    environment: production
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: ./.github/actions/setup
      - run: pnpm build
      - run: pnpm exec wrangler deploy
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
```

`package.json` の scripts に足す（spec §4.1 の `deploy`。手元から本番へ出す場合に使う。通常のリリースはタグの push による）:

```json
"deploy": "pnpm build && wrangler deploy"
```

- [ ] **Step 3: 手元で確かめる**

Run: `node -e "for (const f of ['.github/workflows/ci.yml', '.github/actions/setup/action.yml']) { const s = require('node:fs').readFileSync(f, 'utf8'); if (/\t/.test(s)) throw new Error(f + ' にタブ') } console.log('ok')" && python3 -c "import yaml,sys; [yaml.safe_load(open(f)) for f in sys.argv[1:]]; print('yaml ok')" .github/workflows/ci.yml .github/actions/setup/action.yml`
Expected: `ok` と `yaml ok`（python3 の yaml が無い環境では後者を省き、その旨を記録する）

Run: `grep -nE 'uses: [^.]' .github/workflows/ci.yml .github/actions/setup/action.yml | grep -vE '@[0-9a-f]{40} #'`
Expected: 出力なし（すべての Action が 40 桁の SHA とタグのコメントでピン留めされている）

Run: `CLOUDFLARE_ENV=staging pnpm build > /dev/null && node -e "console.log(require('./dist/wrangler.json').name)" && pnpm exec wrangler deploy --dry-run | tail -3 && pnpm build > /dev/null && node -e "console.log(require('./dist/wrangler.json').name)"`
Expected: `raintrace-staging`、dry-run が成功、`raintrace`

- [ ] **Step 4: Commit**

```bash
git add .github
git commit -m "CI/CD: 品質ゲート、PR のプレビュー、main でステージング、タグで本番"
```

---

### Task 9: 実測の反映と引き継ぎ

**Files:**
- Modify: `specs/tech-spec.md`（§14.2 のチャンク別の予算 = T6、§10 に TypeScript の版の注記）
- Create: `.handoff/README.md`、`.handoff/01-foundation.md`（git 管理外）

- [ ] **Step 1: tech-spec §14.2 にチャンク別の予算を書く（T6）**

Task 7 の `pnpm size` の実測を表にし、予算を「実測 + 余裕」で決めて §14.2 の「チャンクごとの予算（目安…）は、Phase 1 の実測で確定する」の段落を置き換える。形式:

```markdown
2026-09-10 の実測（spec 01 の完了時、gzip 後、1KB = 1000 バイト）とチャンク別の予算:

| チャンク | 内容 | 実測 | 予算 |
|---|---|---:|---:|
| `map` | maplibre-gl | （実測） KB | （実測を 10KB 単位で切り上げ + 10） KB |
| `ui` | react、react-dom、@mui、@emotion とその依存 | （実測） KB | 同上 |
| `index` | アプリ本体 | （実測） KB | 同上 |
| 初期ロードの合計 | | （実測） KB | 400 KB |
| `maplibre-gl-worker` | MapLibre の内部 Worker（起動時に読まれるが、初期ロードの定義の外） | （実測） KB | — |
| 総量 | | （実測） KB | 1.2 MB |

チャンク別の予算は目安であり、CI が検査するのは初期ロードと総量の上限である。
```

（「（実測）」は Task 7 で控えた数字に置き換える。予算の決め方の式もそのまま書く）

- [ ] **Step 2: tech-spec §10 に TypeScript の版を注記する**

§10.3 の末尾（「2026-09-10 に tsc 5.9.3 で…」の段落の後）に足す:

```markdown
TypeScript は 6 系を使う（2026-09-10 時点で 6.0.3）。7 系（Go 実装）は npm パッケージが従来の JS API を公開しておらず、dependency-cruiser（§4.2）が対応していない（18.2.0 は `typescript >=2.0.0 <7.0.0` のみ）。dependency-cruiser が対応した時点で 7 系への移行を検討する。
```

- [ ] **Step 3: Commit**

```bash
git add specs/tech-spec.md
git commit -m "tech-spec: バンドルの実測とチャンク別の予算（T6）、TypeScript の版"
```

- [ ] **Step 4: 引き継ぎのメモを書く**

superpowers の writing-pr-descriptions のスキルで PR 本文を書く。

`.handoff/README.md`（ブランチの順番と、ユーザーが実行するコマンド。spec ごとに追記していく）:

````markdown
# push と PR の手順

縦積みのブランチを、下から順に push して PR を作ります。各 PR の base は1つ下のブランチです（01 は main）。
下の PR がマージされたら、GitHub が次の PR の base を自動で main に付け替えます（付け替わらない場合は手で変更）。

| 順 | ブランチ | base | PR 本文 | 状態 |
|---|---|---|---|---|
| 1 | `feat/01-foundation` | `main` | [01-foundation.md](01-foundation.md) | レビュー承認済み／push 待ち |

## 01 の push の前に（初回のみ）

1. Cloudflare の API トークンを作る（テンプレート「Edit Cloudflare Workers」相当、Workers のデプロイ権限のみ）
2. GitHub の Secrets（リポジトリ）に `CLOUDFLARE_API_TOKEN` と `CLOUDFLARE_ACCOUNT_ID` を登録する
3. GitHub の environment `staging` と `production` を作る
4. （任意）PR のプレビューはステージングの Worker（`raintrace-staging`）に上げるため、Worker がまだ無いと 01 の PR の `deploy-preview` は失敗する。先に作る場合は、手元で `wrangler login` のうえ `CLOUDFLARE_ENV=staging pnpm build && pnpm exec wrangler deploy`

## コマンド

```sh
git push -u origin feat/01-foundation
gh pr create --base main --head feat/01-foundation --title "<PR 本文の1行目>" --body-file .handoff/01-foundation.md
```

## マージ後

- `main` のブランチ保護で、必須チェック `quality`・`test`・`build`・`e2e` を設定する
- ステージングの URL で地図が表示され、応答に CSP が付くことを確かめる
- 本番へのリリース: `git tag v0.1.0 && git push origin v0.1.0`
````

`.handoff/01-foundation.md`: PR 本文。少なくとも次を含める:
- 概要（spec 01 の目的と、PoC 完成条件 #1）
- spec 01 §4.5 の一度だけの検証 4 件の結果（Task 6 Step 4）
- `allowBuilds` に加えたパッケージと理由（Task 3 Step 2、Task 6 Step 1）
- `pnpm config get minimumReleaseAge` の確認（Task 1 Step 3）
- バンドルの実測（Task 7）
- spec との差異: TypeScript 6（7 は不可）、ステージングは `--env staging` ではなく `CLOUDFLARE_ENV=staging` のビルド、プレビューはステージングの Worker に上げる、`wrangler.jsonc` に `assets.directory` を書かない（プラグインが決める）、`scripts/lib/` と `tsconfig.base.json` と composite action の追加、MapLibre の Worker を `setWorkerUrl` で渡す、`useImportExtensions` の Biome の設定の形
- 02 への申し送り（WebKit の調査結果、Task 4 Step 7）
- push 後に確かめること（完了条件 1・2・7・8: CI の成功、プレビュー・ステージング・本番の表示、Firefox と Safari の手動確認、CSP のヘッダ）

- [ ] **Step 5: レビュー役にレビューを依頼する**

SendMessage でレビュー役（`raintrace-ab`。名前は ListAgents で確かめる）に、ブランチ名、コミットの一覧（`git log --oneline main..HEAD`）、`.handoff/01-foundation.md` の場所を送り、spec 01 と本計画に照らしたレビューを依頼する。指摘は superpowers:receiving-code-review に従って検証してから反映する。承認が出たら `.handoff/README.md` の状態を「レビュー承認済み／push 待ち」にする

## spec 01 の完了条件との対応

| 完了条件（spec §7） | 満たす Task | 確認する時点 |
|---|---|---|
| 1. PR で必須ジョブが成功し、プレビュー URL で地図が表示される | 8 | push 後（ユーザー） |
| 2. main でステージング、タグで本番に配信される | 8 | マージ後・タグの push 後（ユーザー） |
| 3. §4.5 の検証の記録 | 6、9 | 実装中 |
| 4. `pnpm config get minimumReleaseAge` が 14400 | 1 | 実装中 |
| 5. `allowBuilds` の記録 | 3、6、9 | 実装中 |
| 6. バンドルの実測とチャンク別の予算を tech-spec §14.2 に反映 | 7、9 | 実装中 |
| 7. Firefox と Safari での手動確認 | — | プレビュー URL ができた後（ユーザーまたは実装役） |
| 8. プレビューと本番の応答に CSP | 4（preview で確認）、8 | push 後 |
